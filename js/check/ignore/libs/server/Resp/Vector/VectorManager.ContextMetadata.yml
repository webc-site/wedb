- 理由: AllocateTestContexts C# 亦零生产调用（测试用途 API），rust 测试未消费；
    ExtractContextFromNamespaces/StoreContextInNamespace C# 迁移路径活跃，
    rust 迁移链路当前走 chunk deserializer 与 publish_migrated_range_index 的
    AlreadyExists 错误通道，无消费点，孤立纯函数删除，待迁移功能补齐时随链路恢复
  文件:
    - libs/server/Resp/Vector/VectorManager.ContextMetadata.cs:
        - AllocateTestContexts
        - ExtractContextFromNamespaces
        - StoreContextInNamespace
