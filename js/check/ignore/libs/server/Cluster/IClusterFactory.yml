# r3-c7-tail 长尾扫尾：CreateCheckpointManager 产出 Tsavorite DeviceLogCommitCheckpointManager；rust 检查点面由 wkv CheckpointManager + databases 域承接（CreateClusterProvider 已实现为 SingleNodeClusterProvider）
fn:
  - "CreateCheckpointManager"
