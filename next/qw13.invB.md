# qw13.invB 队列盘点（批次 B：wkv/windex/whlog/waof/wcpr/wbftree/wreviv/wcol/whyperlog/wdev/wbase/wtxn/wtest_base/bench）

只读盘点，判据均按符号在 dev 复核，不采信票内行号。入场基线 bfb3cecd，会话期间 dev 前进至 6e008a33，关键结论（aof_processor.rs:1188、wtxn STRIPE_COUNT、array_commands slow::del、windex PREFETCH_WINDOW、ObjectOutput、rmw_helpers 重灌序、stub.rs:188）已在新 HEAD 逐条复测无变。

（全部判完，判据逐票带 file:line 证书；结尾「汇总」段给四态计数与撞车清单）

立刻可派的 open/partial 前 5（按后果重轻）
1. task/ing/aof-replay-virtual-domain-context.md —— 跨租户串数据/回放写脏 DbMeta，单棒（wnode/src/aof + wkv/src/session）
2. task/ing/rmw-atomic-read-modify-write-window.md（并 string-rmw-key-bucket-lock.md + wtxn-lock-stripe-count-parity.md 为一棒）—— 并发同键静默丢更新
3. task/ing/tiered-drain-envelope-tombstone.md（并 tiered-reflush-atomic-swap.md，先墓碑后换序）—— 幽灵集合复活 / 重灌丢键
4. task/ing/checkpoint-dir-retention-wiring.md —— 快照目录单调增长致磁盘耗尽（wnode/src/database + wcpr + wconf）
5. task/ing/msetnx-slow-path-meta-domain-probe.md + task/ing/del-unlink-vector-registry-slow-arm.md —— 同为写坏存储态的小改点，可并一棒（均在 wnode/src/resp，须与另一代理的 slow-path 票协调同文件）

---

task/ing/aof-replay-virtual-domain-context.md | open | 判据：KeyContextGuard::enter/drop 改 set_virtual_context 直设物理域。现状仍为逻辑域入口：wedb/wnode/src/aof/aof_processor.rs:1188 `batch.set_context(ns, db)`、:1200 drop 侧同用 `set_context`；分块回放 wedb/wnode/src/aof/aof_processor_chunk_replay.rs:100 同用该守卫；FLUSH 族回放臂亦未新增物理域入口（wedb/wkv/src/store/keyspace.rs 侧 flush_database 仍先 resolve_context）。全仓 grep `set_virtual_context` 在 wnode 回放面零命中（仅 gc.rs/keyspace.rs 与注释）。 | 重派：wnode/src/aof + wkv/src/session 域，单棒（票自述跨两层、勿与向量登记表域隔离并档）。无在跑分支撞车（fix-* 无 aof-replay 相关）。高优先（正确性面）。

task/ing/aof-store-rmw-dead-replay-arms.md | done | 判据「四臂删净 + default 显式失败 + 连带零消费面普查」全部成立：全仓 grep `RespCommand::{Setex,Psetex,Getdel,Expireat} =>` 零命中；证据 wedb/wnode/src/aof/aof_processor.rs:1028-1054 删除声明注释已覆盖 Expireat/Getdel/Setex/Psetex 四形态并声明 StringRMW 臂与 rmw_main_store 收口同批删除（grep `rmw_main_store` 仅命中注释与测试自述 wedb/wnode/tests/aof_store_rmw_replay.rs:183）；default 臂 aof_processor.rs:1081-1094 显式失败；测试断言错误串 wedb/wnode/tests/aof_store_rmw_replay.rs:188-219（Expireat/Setex 形态入参）；连带面：wedb/wnode/src/storage/session/mainstore/main_store_ops.rs 的 getdel 已消失（仅剩 :25 setex，与票「保留」一致）；wbase 侧 duration_seconds_to_ticks 仍在 wedb/wbase/src/convert.rs:144 并有消费（:168），符合「只删 import 不删函数」。 | 不重派，建议归档 task/done。

task/ing/checkpoint-dir-retention-wiring.md | open | 判据：wcpr purge_outdated 有生产调用 + wconf 保留旋钮承载位 + 恢复后清未用。现状：`purge_outdated`/`purge_all` 全仓命中仅定义（wedb/wcpr/src/manager/mod.rs:496、:521）、导出（wedb/wcpr/src/lib.rs:60）、测试（wedb/wcpr/tests/cpr/token_layout.rs:79、wedb/wkv/tests/checkpoint/checkpoint_manager.rs:79）与 README 文案，零生产调用；主库打点链 wedb/wnode/src/database/database_manager_base.rs:186 take_database_checkpoint_async、:120 recover_database_checkpoint_async 内 grep `purge|retain|keep|retention` 零命中；wedb/wconf/src 全域 grep `retention|checkpoint_keep` 零命中。 | 重派：wnode/src/database + wcpr + wconf 域，单棒。无 fix-* 分支撞车。

task/ing/checkpoint-flush-throttle-knob.md | open | 判据（二择一须留凭据）：wconf 补 checkpoint-throttle-delay 槽位并在 whlog flush_pages_range 段落节流；或 js/check/ignore 登记不移植理由。现状两条皆无：wedb/wconf/src grep `throttle` 零命中；js/check/ignore/ 全域 grep `CheckpointThrottleFlushDelayMs|checkpoint-throttle|throttleCheckpointFlushDelayMs` 零命中；刷盘链仍无参 flush_all（wedb/wcpr/src/manager/create.rs:227 → wedb/wkv/src/store/flush.rs:114 → wedb/whlog/src/hlog/io.rs flush_pages_range）。 | 重派：低档，可并入任一 whlog/wcpr 刷盘改造批（票自述与 flush-safe-read-only-bound 同处一条刷盘链，须同改 io.rs）。单棒或与刷盘边界票并棒。

task/ing/del-unlink-vector-registry-slow-arm.md | open | 判据：向量集清退下沉到存储删除单点，或 slow::del 加 vector 形参并在分派点接线。现状两套并存未收口：快臂 wedb/wnode/src/resp/array_commands.rs:186 `!deleted && vector.is_some_and(|vm| vm.delete_vector_set(...))`；慢臂同文件 :771-783 `async fn del(storage, refs, output)` 无 vector 形参、:778 单臂 `storage.delete_string(key)`；分派点 wedb/wnode/src/resp/garnet_api/slow.rs:599-606 不传 vector；单点收口也不成立（wedb/wnode/src/storage/session/storage_session.rs:363 fn delete_string 不触碰登记表）。注意在跑/历史分支 fix-del-unlink-slowarm 的 tip 只是文档分拣提交（7be9c56b），未落地本修法。 | 重派：wnode/src/resp + storage/session 域。分支 fix-del-unlink-slowarm 名存实亡（tip 已并入 dev 且无本域改动），可安全重派，建议先清该陈旧 worktree/分支归属。

task/ing/reviv-knobs-zero-production-wiring.md | open（dev 上未落地，活体在未合分支） | 判据：wconf/service.rs 有 reviv/copy_reads_to_tail 生产投影、record_elision 门撤除、会话不再有第二套真源。现状 dev：wedb/wconf/src grep `reviv|copy_reads` 零命中；wedb/wnode/src/service.rs:676 apply_hlog_overrides 仍只投 page_size/memory_size/mutable_fraction/read_cache；wedb/wkv/src/session/mod.rs:160/:161 两开关仍硬编码 AtomicBool::new(false)，:410/:416/:422/:428 会话级 setter 仍在；record_elision 门读者仍在本仓（wkv/src/store/hlog_scan.rs:17 自陈「默认关闭」）。分支 fix-reviv-knobs 未并入 dev（tip: fix(reviv): 冷读晋升与复活旋钮打通生产装配链）。 | 重派：先收尸/复核 fix-reviv-knobs 分支载荷再决定补棒，勿另开同题第二棒；同域 wkv/src/session + wconf + wnode/service.rs。票自述执行序须排在 string-rmw-key-bucket-lock、rmw-atomic-read-modify-write-window 之后。

task/ing/windex-prefetch-window-dual-impl.md | open | 判据：预取核收口为 windex 单点、wkv 删本地 12 常量与手写下钻、白盒谓词收 cfg(test)、README 实名修正。现状：两处 12 常量并存 wedb/windex/src/table.rs:48 PREFETCH_WINDOW 与 wedb/wkv/src/session/raw/batch.rs:15 BATCH_READ_PREFETCH_SIZE；wkv 仍手写 wedb/wkv/src/session/raw/batch.rs:265 prefetch_batch_probes（:118/:235 消费）；windex 批量入口 wedb/windex/src/table.rs:623 find_tag_batch_by_hash、:698 acquire_hash_locks 生产零消费者（命中仅 wedb/windex/tests/main.rs:141、tests/index/latch_concurrency.rs:470/:498/:659）；文档漂移未修：wedb/windex/README.md:33、:79 与 readme/en.md:21、readme/zh.md:22 仍写不存在的 find_tag_batch / lookup_candidates_batch。注意 wnode 已消费 BATCH_READ_PREFETCH_SIZE（wedb/wnode/src/resp/vector/vector_store_callbacks.rs:35/:86/:95），收口面比票面更宽。 | 重派：windex/src/table + wkv/src/session/raw/batch 域，单棒；分支 fix-windex-prefetch 的 tip 是无关旧合并（6c60d847），未承接本题，可重派。
  撞车加证：盘点收尾时该棒正在主工作树里改（git status 显示 wedb/windex/src/table.rs、windex/src/prefetch.rs 新模块、wedb/wkv/src/session/raw/batch.rs -108/+…）——即预取核正上提到 windex 单点、wkv 改调该核，与票面方向一致但尚未提交。本票此刻严禁另派，等 dev-prefetch 落定后按结果改判。另证：真正载荷在未合分支 dev-prefetch（tip 198639db「refactor(windex): 删除零消费者的 PREFETCH_WINDOW 滑窗预取面，12 项预取收口 wkv 批量读单点」，改 table.rs/tests/main.rs/wkv batch.rs/README 四组件），worktree /tmp/fork/dev-prefetch 在跑。本票判 dev 上 open 但不可重派——先等 dev-prefetch 合入或收尸，其方向与票面「收口为 windex 单点」相反（删 windex 死面、留 wkv 单点），合入后本票可按「单源达成」归档，剩余残留只在 windex/README 实名与 acquire_hash_locks 零消费面。

task/ing/wtxn-lock-stripe-count-parity.md | open | 判据：删第二套条带锁表改由桶锁承接（或至少条带数随索引规模联动），且订正 wedb/wtxn/src/txn_lock_table.rs:21 的「C# Tsavorite 锁表默认 64K 桶」虚构注释。现状：虚构注释原文仍在 :21-23，:23 STRIPE_COUNT = 1<<10、:28 Arc<StripedLatch<STRIPE_COUNT>>、:67-68 stripe_index_for_hash 与 size_mask 无关；排序/加锁仍同建立在常量条带上 wedb/wtxn/src/txn_key_entry_comparison.rs:33-34、txn_key_entry.rs:136，导出 wedb/wtxn/src/lib.rs:32。 | 重派：wtxn + windex 桶锁域。与 string-rmw-key-bucket-lock / rmw-atomic-read-modify-write-window 同主题（共用「windex 桶闩为唯一锁源」结论），建议并成一棒或按 rmw → string-rmw → wtxn 顺序串棒。

task/ing/info-resetstat-gossip-reviv-reset-arms.md | open | 判据：reset_gossip_stats / reset_revivification_stats 各有生产调用点 + wreviv 补 reset_stats + 失真注释删。现状：全仓 grep `reset_gossip_stats` 只有三处定义/转发（wedb/wedb/src/server/cluster_provider.rs:1657、wedb/wnode/src/cluster_provider.rs:135 默认空实现、:270 Arc 转发），零调用点；wedb/wnode/src/database/single_database_manager.rs:166-168 函数体仍只剩注释「wkv 无复活化统计面……空操作」（与 wkv/src/store/mod.rs 持 reviv_pool 的事实相反）；全仓 grep `reset_stats` 在 wedb/wreviv/src 零命中（池层无复位原语）；监视器消费面 wedb/wmetric/src/garnet_server_monitor.rs 亦无两臂挂点。 | 重派：wmetric + wnode/src/servers/consumer_registry + wreviv/src/pool 域，改动面小可独立成棒；与 reviv-knobs 不同面（那是装配、这是复位）。注意票自述 next/ 同名派发档双花，编排只取一。

task/ing/rmw-atomic-read-modify-write-window.md | open | 判据：wkv 提供命令级原子 RMW 入口（锁内读-算-写）+ 命令层两步形态删净 + 并发同键回归用例。现状：全仓 grep `rmw_user_key_atomic` 零命中；acquire_keys_lock_exclusive 生产消费仍只有 wedb/wkv/src/ttl.rs:454、:502 两处（EXPIRE/PERSIST 先例未扩至 RMW 族）；两步形态原样在 wedb/wnode/src/resp/basic_commands/incr.rs:97 read_user_sync → :123 store.try_rmw_sync（同文件 :156/:193 浮点同型）；盲写口未收（wedb/wnode/src/storage/session/storage_session.rs:347 rmw_string，try_rmw_sync/upsert_rmw 仍在）；wedb/wnode/tests 无并发同键读改写用例（仅 aof_store_rmw_replay.rs、ttl_rmw_semantics.rs、vector_set_concurrent_vadd_disk_spill.rs 等不同靶）。 | 重派：wkv/src/session/raw + wnode/src/resp/{basic_commands,bitmap,hyperloglog,objects} 域，本票是收口单。与 task/ing/string-rmw-key-bucket-lock.md 同题三份双花（第三份 rmw-same-key-atomicity.md 已剪），必须只派一棒，且按票自述排在 wtxn-lock-stripe-count-parity 之后或同批。

task/ing/string-rmw-key-bucket-lock.md | open（与 rmw-atomic-read-modify-write-window 同题重复票） | 判据同上：唯一锁源收口到 windex 桶闩、禁新建私有锁表。现状：wkv 侧无新原子入口，桶锁无新消费者（见上票证据），wtxn 条带表仍独立（wtxn/src/txn_lock_table.rs:23、:28）。本票三项独占增量（user_read.rs 读侧无锁取证、bucket.rs 桶闩对位、禁与 wtxn 条带表并联）在派发时须转录进收口单。 | 重派：不单独派，与 rmw-atomic-read-modify-write-window.md + wtxn-lock-stripe-count-parity.md 并成一棒（三票同一把锁），建议编排直接合并票面后派一次。

task/ing/msetnx-slow-path-meta-domain-probe.md | open | 判据：慢路径存活判定改调 StorageSession::exists（三域）或补 KeyTag::Meta 第三探。现状 wedb/wnode/src/resp/garnet_api/slow.rs 的 `C::Msetnx` 臂仍只探两域：`read_tag_with(key, KeyTag::String, ..)`、`read_tag_with(key, KeyTag::ObjectEnvelope, ..)`，grep 该臂内 `KeyTag::Meta|storage.exists` 零命中。 | 重派：wnode/src/resp/garnet_api/slow.rs 域；票自述与 slow-path-string-key-admin-arms.md 同函数（exec_slow_impl）宜同棒以免互顶行号——该他票属另一代理范围，派单时须协调。

task/ing/range-index-replication-shutdown-dispose.md | open | 判据：方向 A（停机链显式调复制面 dispose）或方向 B（删死面 + 文档声明），二者必落其一。现状：dispose() 仍零生产调用（wedb/wnode/src/rangeindex/range_index_manager_replication.rs:597 为函数体内自调 engine.dispose()，全仓 grep `.dispose()` 无 ri/范围索引挂点）；停机链 wedb/wnode/src/server.rs:776 stop 内只有 dispose_vector_cleanup(:781)、aof 背压 bp.dispose(:791)、dispose_pubsub(:799)，无范围索引步；:328 单机尾部仍只有 cluster_provider.dispose()。 | 重派：wnode/src/server.rs + rangeindex 域，与订阅 broker 收口同批（pubsub 那面已落地 wedb/wnode/src/service.rs:1689 dispose_pubsub，可参照其形态）。

补充：wkv 七条历史红测试的归属线索（只读取证）
- 位点：wedb/wkv/tests/store/flush_database.rs:578 test_flush_atomic_batch_survives_rebuild、tests/store/dbmeta_layout.rs:25 cold_resolve_persist_survives_rebuild_and_probe、tests/store/swap_database.rs、wedb/wkv/src/vdb.rs 内联 dbmeta 用例，四条同属「换号 + DbMeta 批落盘」面；read_cache 三条在 wedb/wkv/tests/store/read_cache.rs（该文件与 src/read_cache 自 init 以来零改动）。
- 与本次盘点票的关系：判 open 的 aof-replay-virtual-domain-context 不成立为成因——它改的是回放端入口（aof_processor.rs:1188），尚未落地即不可能引入红的变化；红更像由已并入 dev 的 52c7e5bc「FLUSHDB 换号批落盘重建闭环测试」+ 02113c67 f19-dbmeta-batch 合并引入（同批改了 keyspace.rs / store/mod.rs）。read_cache 三条与 reviv-knobs / 热更装配无关（wedb/wconf/src 无 reviv 投影，wedb/wnode/src/service.rs:676 apply_hlog_overrides 仍只投 page/memory/mutable_fraction/read_cache 四项，read_cache 装配是 b304c5ee 热更消费者批引入）。结论：这七条红属 dev 侧已落地代码的既有失败，需独立的修红票，不能靠重派上述 open 票消解。

task/ing/tiered-drain-envelope-tombstone.md | open | 判据：排空单点连带写信封域幂等墓碑 + promote 删信封失败不再 `let _ =` + collection.rs 头注订正。现状：wedb/wkv/src/range_index/stub.rs:50-66 handle_bftree_drain_and_delete 仍只做 drain_and_delete_collection_meta + delete_index + unregister_bftree_key + RangeIndexDrop，无信封臂；promote 尾仍 `let _ = self.delete_raw(&env_k).await;`（stub.rs 内 :188 段）静默吞错；wedb/wkv/src/session/collection.rs 头注仍自陈「落元记录与删信封同帧完成」，与两条 await 实现矛盾。 | 重派：wkv/src/range_index + wkv/src/session 域。与 tiered-reflush-atomic-swap.md 同函数相邻，票自述须串行（本票先落）。无 fix-* 撞车。

task/ing/tiered-reflush-atomic-swap.md | open | 判据：重灌改先建后拆、复用 publish_tree_from_snapshot_locked(replace=true) 单内核，且不再在重灌臂调 handle_bftree_drain_and_delete(…, true)。现状：仍先拆后建，代码已从票面位点 object_store_utils.rs 搬到 wedb/wnode/src/resp/objects/rmw_helpers.rs:377-391（tiered 时 :383 handle_bftree_drain_and_delete(key, true) 后再 :390 promote_collection_to_bftree）；publish_tree_from_snapshot_locked 生产消费者仍只有 wedb/wkv/src/range_index/migration.rs:50 一处。 | 重派：wnode/src/resp/objects/rmw_helpers + wkv/src/range_index 域，排在 tiered-drain-envelope-tombstone 之后（两票并成一棒亦可，先墓碑后换序）。派单须提醒子代理票内行号指向的文件已拆分，落点在 rmw_helpers.rs。

task/ing/zset-aggregate-member-ttl-filter.md | open | 判据：wcol 补存活视图口（对位 C# Dictionary getter）+ 聚合内核改走该口。现状：全仓 grep `live_dictionary` 零命中；wedb/wnode/src/resp/objects/sorted_set_commands/ 目录 grep `is_expired` 零命中（命令层无成员级过期裁决）；内核仍裸遍历 wedb/wnode/src/resp/objects/sorted_set_commands/write.rs:899 diff_sets、:920 combine_sets、:362 sorted_set_intersect_length。 | 重派：wcol/src/zset + wnode/src/resp/objects/sorted_set_commands 域，与 zset-o1-bypass-expiry-count.md 同文件不同面（票自述禁互抄计数口径），可同代理两棒或并成一棒；无 fix-* 撞车。P1 数据正确性。

task/ing/object-output-payload-direct-write.md | open | 判据：ObjectOutput 改 `payload: &'a mut Vec<u8>` + mount/base/written/reset 挂载口。现状 wedb/wcol/src/resp/output.rs:29-31 仍 `pub struct ObjectOutput { pub payload: Vec<u8>, … }`，grep 该文件 `mount|base|reset` 零命中。/tmp/fork 有历史 worktree 名 f61-obj-output，但 dev 无载荷（不在 fix-* 分支列表）。 | 重派：wcol/src/resp/output + wnode 对象命令构造点域，打磨档，可后置。

task/ing/readme-crate-map-drift.md | open | 判据：全仓 readme*/README* 无 wram / ext_noop / fifteen / 十五个 / float / 「16 分片」残留 + 副本单源化 + 根 README 渲染。现状全部仍在：readme/zh.md:92、:106（ext_noop、「16 分片无争用网络缓冲池」）、readme/en.md:80、:92、:106（16-way sharded）、readme/zh/README.md 与 readme/en/README.md 同文重复（cmp 判 readme/zh.md 与 readme/zh/README.md 字节同）；wedb/README.md 命中 wram 16 处、wedb/readme/{zh,en}.md 各 7 处；根 README.md 与 README.mdt cmp 判完全相同（未渲染模板）。 | 重派：纯文档棒（readme/ + README.mdt + sh/dist.sh 再生成），零代码冲突风险，可立刻派；须与另一代理的文档票避开同一文件（gate-anchor-drift 只碰注释锚，不碰 README）。

task/ing/now-stopwatch-ticks-wall-clock.md | open（撞车提示见末段） | 判据：计时源换单调基（Instant/quanta）或反向收口（注释改实时域 + 消费点回拨防护）。现状：wedb/wbase/src/time.rs:42-44 `now_stopwatch_ticks()` 仍 `now_nanos()/NANOS_PER_TICK`（:31-33 now_nanos 为 Clock::now_since_epoch 实时域），:38 注释仍自陈「100ns 单调域」；消费点未加防护 wedb/wnode/src/resp/metrics_commands.rs:109 `let elapsed = now_ticks - self.slow_log_start_ticks`（裸减）与 wedb/wnode/src/resp/resp_server_session.rs:1094、:1110 `as u64` 直转。git log 显示 wedb/wbase/src/time.rs 自 init 零改动；分支 fix-now-stopwatch 的 tip 只是把 dev 并回分支的合并（5ed717e5），无本域载荷。 | 重派：wbase/src/time + wnode 计时消费点域，单棒；分支名 fix-now-stopwatch 为空壳，可安全重派（建议编排顺带清理该分支/worktree 记账）。

task/ing/zset-o1-bypass-expiry-count.md | open | 判据：count() 在册分支不再遍历 expiration_times（旁路抵扣标量或堆序 purge）。现状：wedb/wcol/src/zset/sorted_set_object.rs:551-558 仍 `times.keys().filter(|k| self.is_expired(k)).count()`；全仓 grep `expired_pending|pending_expired` 在 wedb/wcol/src 零命中（无抵扣标量）。 | 重派：wcol/src/zset 域，与 hash 半边（next/hlen-o1-bypass-expiry-count.md，非 ing 在册）同口径一次收口；票自述禁两套计数口径，若 hash 侧另立票须同代理。

task/ing/gate-anchor-drift-reclean.md | partial | 判据一（改虚构锚点）未做：wedb/wnode/src/resp/objects/tiered_collection_ops.rs 仍挂 `libs/server/Objects/Hash/HashObjectImpl.cs` 的 Set 断言（现位点 :609 段「插成功才计数、才回 1」，票面 :91 已漂移），且 C# 侧 grep `public .*Set(` 于 garnet/libs/server/Objects/Hash/HashObjectImpl.cs 零命中 → 锚点仍虚构。判据二（19 族逐族甄别）大部已在：js/check/ignore/ 已覆盖 LightEpoch(3 件)/AllocatorBase（storage.yml:2 块）/ObjectAllocator/SpanByteAllocator/TsavoriteLogAllocator/IAllocator/LogAddress/AofAddress/DoubleTurnstileBarrier/OverflowBucketLockTable/ProtectionTests/ScanIteratorEpochFailureTests/BuggyLightEpoch/ReplicaSyncSession，唯 PrimarySync 零登记。残留：PrimarySync 族分流 + 第 1 步改锚 + 门是否复绿未证（本盘点禁跑 bun js/check.js）。 | 重派：wnode/src/resp/objects 注释 + js/check/ignore 域。分支 gate-anchor-drift-reclean 与 salvage-gate 均已并入 dev 且 worktree /tmp/fork/gate-anchor-drift-reclean 仍在（unmerged=0，即空壳），可原地续做或重派，无撞车。

task/ing/wreviv-test-record-alignment-second-source.md | open | 判据：夹具常量引 wrecord 单点或剪掉对标声称。现状：wedb/wreviv/tests/reviv/support.rs:9 仍 `pub const RECORD_ALIGNMENT: u32 = 8;`，生产单点仍为唯一另一处 wedb/wrecord/src/header.rs:57（全仓 `const RECORD_ALIGNMENT` 命中仅此两处），wedb/wreviv/Cargo.toml 无 wrecord dev 依赖。 | 重派：wreviv/tests + wrecord 公开面域，打磨档；票自述须排在任何触碰 wreviv/wrecord 公开面的在途票之后——本波 dev-prefetch/fix-reviv-knobs 不碰 wrecord，可派。

task/ing/snapshot-chunk-pooled-borrow.md | open | 判据：段流 trait 返回 AlignedBuf、HLOG 源删 to_vec、检查点文件源改池取缓冲免零初始化。现状：wedb/wedb/src/server/replication/snapshot_transmission.rs:281 trait 仍 `Result<Vec<u8>, String>`、:306 实现同型、:314 仍 `let chunk = buf[..].to_vec();`、:365 仍 `read_exact_at(vec![0u8; want], offset)`。 | 重派：wedb/src/server/replication + wbase/src/pool/aligned_buf + wdev/src/device 域，单棒；注意与 replication-send-path-byte-cap、replica-* 等他代理票同文件（snapshot_transmission.rs），派单前须与另一代理协调避免同文件双改。

task/ing/replication-comment-anchor-drift.md | open | 判据：`TsavoriteSnapshotReader.cs` 与 `m4-checkpoint-import` 在 wedb/ 下零命中。现状四处俱在：wedb/wedb/src/server/replication/snapshot_transmission.rs:5（假文件名）、:8 与 replica_sync_session.rs:124、replica_diskbased_sync.rs:65（三处死文档引用）。 | 重派：纯注释棒（wedb/src/server/replication 三文件），与 snapshot-chunk-pooled-borrow 同文件 → 必须同棒或串行（建议并给同一代理：先注释票后缓冲票，避免顶行号）。

task/ing/qcode10-parse-db-index-i32-parity.md | done | 判据：parse_db_index 收 i32 域 + 三处命令臂按 C# 文案分档 + 线面断言。现状：wedb/wbase/src/num.rs:174-180 `pub fn parse_db_index(raw: &[u8]) -> Result<i32, DbIndexError>`，内部走 strict_i32 并分 NotInteger/OutOfRange 两档；文案单点在 wedb/wnode/src/resp/array_commands.rs:413（「按 C# NetworkSWAPDB 回 invalid first DB index 档」）；线面断言在 wedb/wnode/tests/resp_tests.rs（grep parse_db_index/invalid first DB index 命中）。合并凭据：dev 提交 7b171efc「Merge branch 'qcode10-parse-db-i32' into dev」。 | 不重派，建议归档 task/done。

task/ing/checkpoint-store-purge-entry-list-csemantics.md | done（本盘点期间落地） | 判据：purge_all_checkpoints_except_entry 收为 `(&self, &CheckpointEntry)`、删 retain/clear 链表改写、补 C# 真实锚点。现状（dev 6e008a33）：wedb/wedb/src/server/replication/checkpoint_store.rs 的签名已是 `(&self, keep: &CheckpointEntry)`，文档段已挂 C# :82/:94/:104 与 :38-57/:52-54 锚点，retain/clear 零命中。凭据：dev 4d02c044「merge: checkpoint-store-purge-entry-list-csemantics」+ d2a3dbb9 归档，票文件已被并发会话移入 task/done/。 | 不重派；注意我入场取证时（bfb3cecd）该签名仍是 Option 形态，属盘期间收口。

task/ing/client-pump-buffer-pool-unwired.md | open | 判据：read_pump 三处 `vec![0u8; READ_CHUNK]` 改池借出并消「超时即丢块」形态。现状：wedb/wconn/src/network/pump.rs:31 READ_CHUNK=16K，:200、:235、:257 三处裸 `vec![0u8; READ_CHUNK]` 原样在场。 | 重派：wconn/src/network/pump + wnode/src/net/handler/drive 域。worktree /tmp/fork/fix-client-pump-pool 存在但相对 dev 零未合提交（空壳），可直接重派。

task/ing/replica-wire-test-callback-in-production.md | open | 判据：AofSyncWire 收成单形（仅 Tcp），FrameSink/CallbackWire/四个 From 整体搬出产线（移入测试 crate 或 cfg 门）。现状： wedb/wedb/src/server/replication/replica_wire.rs:56 `pub enum FrameSink` 与 :108/:114 的 From 实现仍在产线路径，测试侧 wedb/wedb/src/server/replication/aof_sync_task.rs:434 仍 `use ...replica_wire::{CallbackWire, FrameSink}`（:473/:492/:508/:528/:540/:588 六处构造）。 | 重派：wedb/src/server/replication 域，与 replica-* 他票同文件须与另一代理协调；无分支撞车（分支列表无 replica-wire 项）。

task/ing/cmd-strings-input-token-single-source.md | open | 判据：wresp::cmd_strings 增设输入 token 常量组并替换生产位点。现状：wedb/wresp/src/cmd_strings.rs:13-43 只有输出帧常量（RESP_OK/RESP_PONG/RESP_EMPTYLIST/RESP_EMPTY/PUBSUB_*），零 `TOKEN` 输入常量；裸字面量位点原样：wedb/wcol/src/types/scan_input.rs:61 `b"COUNT"`、wedb/wcol/src/zset/sorted_set_object_impl.rs:516 `b"LIMIT"`、:538 `b"WITHSCORES"`、wedb/wcol/src/list/list_object_impl.rs:511 `b"COUNT" | b"count"`、wedb/wnode/src/resp/rangeindex/resp_server_session_range_index.rs:495 `b"COUNT"`。 | 重派：wresp/src/cmd_strings 主责（另一代理半区），我域只覆盖 wcol 四点替换 → 建议整票派给 wresp 侧代理一次做完，本侧不并派以免双改。

task/ing/resp-frame-literal-single-source.md | open | 判据：13 处裸拼帧位点改走 wresp 单点。现状样本仍在：wedb/wnode/src/resp/hyperloglog/hyper_log_log_commands.rs:268-270 `output.push(b':') + extend_from_slice(b"\r\n")` 手写整型帧；wedb/wedb/src/server/cluster_session/basic.rs:349 `output.extend_from_slice(b"$0\r\n\r\n")`（票面 :250 已漂移到 :349）。 | 重派：跨域票（wresp/wnode/wedb/wmetric/wcol/wlua/wext_json 各有位点），主责归网络/协议半区代理；我域仅 hash_object_impl.rs:553 一处切片，勿双派。

task/ing/zero-consumer-surfaces-batch-two.md | open | 判据：清单内符号 grep 归零或转活链。抽核全部未动：wedb/wnode/src/resp/basic_commands/mod.rs:89 network_ping、wedb/wnode/src/aof/garnet_log/commit.rs:92 initialize_if、wedb/wmetric/src/info/garnet_info_metrics.rs:1223 get_info_metrics、wedb/wacl/src/user_handle.rs:39 try_set_user（仅同文件测试 :50 消费）、wedb/wcol/src/itembroker/collection_item_broker.rs:242 set_spawner（仅 :335 文档自指）。 | 重派：跨域普查批（主战场 wnode/wresp/wacl/wmetric），由另一代理主责一次批删；我域只 collection_item_broker 一口，建议并入该批勿单开。

task/ing/json-commands-file-split-anchor-decl.md | open | 判据两步：头标改「2 对位 + 19 扩展」两段式声明、json_commands.rs 按命令族拆到单文件不逾 500 行。现状：两步皆未做——文件头 wedb/wext_json/src/json_commands.rs:1 仍是「对标 modules/GarnetJSON/JsonCommands.cs 与 JsonModule.cs」整体对标口径（grep `RedisJSON 兼容|扩展面|无对位|19 条` 零命中），全仓 `grep -c '.cs:'` 该文件 8 处锚点未分型；文件仍单件 1727 行，无 json_commands/ 目录（ls 只见 json_commands.rs）。 | 重派：wext_json 域（我半区边界件），独立小提交先做第一步；worktree 列表无该题分支，可派。

task/ing/wdev-segmented-device-mutator-knobs.md | open（本盘点期间新入队件，原在 next/） | 判据：九口删净、设备参数只经 new/with_pool 构造注入 + js/check/ignore 登记。现状：wedb/wdev/src/segmented_device.rs 九口全部在场（:263 set_capacity、:275 set_read_only、:282 set_preallocate、:289 set_delete_on_close、:296 is_read_only、:302 is_preallocate、:308 is_delete_on_close、:332 dir_sync_count、:344 segmented），生产侧 grep `set_preallocate|set_delete_on_close|dir_sync_count` 在 wedb/wnode/src 与 wedb/wedb/src 零命中。 | 重派：wdev + wnode/src/service.rs 装配域，单棒；分支 fix-wdev-knobs 与 worktree 均在但相对 dev 零未合提交（空壳），可原地续做或重派。

task/ing/cluster-provider-file-split.md | 越界不判 | 主战场 wedb/wedb/src/server/cluster_provider.rs + wnode cluster 面，正文只在比对中提及 wbase；归网络/集群半区。
task/ing/tiered-collection-ops-file-split.md | 越界不判 | 位点 wedb/wnode/src/resp/objects/tiered_collection_ops.rs 纯搬家拆分，归对象/协议半区。
task/ing/pending-lat-no-timing-site.md | 越界且已收口 | 主战场 wconn/wmetric/wnode；本盘点期间已被移入 task/done/pending-lat-no-timing-site.md（dev a0936946 归档提交）。

## 汇总

盘点范围：task/ing 54→58 张（会话期间 dev 由 bfb3cecd 前进到 6e008a33，队列有增删），我半区（wkv/windex/whlog/waof/wcpr/wbftree/wreviv/wcol/whyperlog/wdev/wbase/wtxn/wtest_base/bench-regress 及正文涉及这些 crate 者）共判 36 张。

- done 3：aof-store-rmw-dead-replay-arms、qcode10-parse-db-index-i32-parity、checkpoint-store-purge-entry-list-csemantics（盘期间落地）
- partial 1：gate-anchor-drift-reclean（19 族 ignore 甄别大部已做，虚构锚点 HashObjectImpl.cs:Set 未改、PrimarySync 族未登记、门未证复绿）
- open 31（含 3 张跨域票建议另一代理主责、2 张越界不判单列）
- stale-doc 0 张纯失真，但 4 张票面字段须订正后再派：reviv-knobs-zero-production-wiring（自述「无 reviv 分支」，实况 fix-reviv-knobs 未合且载荷在分支上）、tiered-reflush-atomic-swap（位点 object_store_utils.rs 已拆至 rmw_helpers.rs）、gate-anchor-drift-reclean（锚点位点 :91→:609、19 族现状）、resp-frame-literal-single-source（basic.rs :250→:349）
- 空壳分支/worktree（tip 已并入 dev 且相对 dev 零未合提交，勿据此判 done）：fix-del-unlink-slowarm、fix-windex-prefetch、fix-now-stopwatch、gate-anchor-drift-reclean、readme-crate-map-drift、f61-obj-output、fix-client-pump-pool、fix-wdev-knobs、cp-purge-csemantics（已真落地，正常归档）
- 真在途未合载荷（禁止重派同题）：dev-prefetch（windex 预取单源收口，覆盖 windex-prefetch-window-dual-impl）、fix-reviv-knobs（reviv 旋钮装配，覆盖 reviv-knobs-zero-production-wiring）、txn-aof-marker-session-wiring（1 提交，wtxn 域无关本批票）
- wkv 七条红：与本半区 open 票无因果关系（回放票未落地即不可能引入；read_cache 面自 init 零改动），线索指向已并入 dev 的 52c7e5bc/02113c67 换号批落盘与 b304c5ee 热更装配，需独立修红票
