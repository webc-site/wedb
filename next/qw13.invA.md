# qw13.invA — ing 票据四态盘点（域：wnode / wedb(server,replication,cluster_session) / wconn / wresp / wacl / wlua / wcustom / wmetric / wconf）

只读盘点，判据基线 = 工作树（HEAD bfb3cecd，与 dev 同树）。格式：
`票 | 态 | 判据+证据 | 重派建议`

## 结论（滚动更新）

task/ing/cluster-migrate-vectorsets-dead-flag.md | done | 判据=删 CLUSTER MIGRATE 头 vectorSets 线参数+接收端丢弃校验+ERR_VECTOR_SET_FLAG 文案。全仓 grep `ERR_VECTOR_SET_FLAG` 零命中；`wedb/wedb/src/client.rs:351-357` `execute_cluster_migrate_async(&self, source_node_id, replace, slot_list, payload)` 无 is_vector_sets，仅 :361 replace_str T/F；接收端注释 `wedb/wedb/src/server/cluster_session/migrate.rs:193` 自述该位已废、kind=5/6 自描述为唯一判据；`wedb/wedb/tests/cluster_migration.rs:912,981-995` 钉死 4 参形状并断旧 5 参形报错 | 删档（fix-vectorsets-dead-flag 已合 dev 656759ec/d4462534）

task/ing/cluster-provider-file-split.md | open | 判据=cluster_provider.rs（1705 行、主 impl 920 行）按 C# 职责切六件。现状 `wedb/wedb/src/server/cluster_provider.rs` 仍 1711 行单文件，`impl ClusterProvider` :226-:1161 独占 935 行，四段异质职责同文件（impl Default :185 / IClusterProvider :1161 / CheckpointCallbackFace :1247 / WnodeClusterProvider :1303），`wedb/wedb/src/server/cluster/` 仅 3 件（mod/i_cluster_provider/cluster_preferred_endpoint_type）未承接任何段 | 立刻可派：wedb/src/server/cluster_provider 纯搬家分件；与在跑 fix-* 无撞车（无同域分支）

task/ing/checkpoint-store-purge-entry-list-csemantics.md | partial | 判据（核心裁决）=purge 只清磁盘孤儿、删 `entries.retain`/`clear` 链表改写，并「Option 形态直接删，不留旧形态」→ `(&self, &CheckpointEntry)`。前半已落地：`wedb/wedb/src/server/replication/checkpoint_store.rs:112-123` 纯转调 purge_checkpoint_files_except，全文件仅 :79 initialize 一处 clear；用例同批改 `:366-394` 断 `entry_count() == 0`（C# 语义）。残留::112 签名仍是 `Option<&Arc<CheckpointEntry>>` + :113-115 `let Some(keep) .. else { return }` 空兜底，未按修法收为必传入参 | 建议轻棒重派（wedb/src/server/replication/checkpoint_store.rs 单文件，签名收口 + initialize 调用点 + 文档锚点复核），非阻塞头

task/ing/cluster-migrate-vectorsets-dead-flag.md（见上）

task/ing/cluster-slot-parse-error-parity.md | open | 判据四条：①NotInteger 臂改写 RESP_ERR_INVALID_SLOT；②InvalidRange 改 tuple 变体 + 新增 write_slot_range_error 动态文案；③删 RESP_ERR_INVALID_SLOT_RANGE；④range 臂判定序「倒挂先于越界」。dev 四点全未动：`wedb/wresp/src/cluster_cmd_strings.rs:17` 自造串仍在；`wedb/wedb/src/server/cluster_session/slot_mgmt.rs:185-194` InvalidRange 仍 unit 变体、:201 NotInteger 臂仍写 RESP_ERR_GENERIC_VALUE_IS_NOT_INTEGER、:203 仍写自造串、:227 越界 先于 :230 倒挂；全仓 grep write_slot_range_error 零命中；`wedb/wedb/tests/cluster_resp_session.rs:1213` 仍钉旧串 | 立刻可派（wedb/src/server/cluster_session/slot_mgmt.rs + wresp/src/cluster_cmd_strings.rs，须同批改 tests/cluster_resp_session.rs:764/1038/1176/1213）。注意：分支 cluster-addslots-slot-parse-parity / dev-slotparse 虽已 merge 入 dev，实为陈旧壳（tip=ea7340bd，无任何 fix 提交），不可当作半成品复用

task/ing/now-stopwatch-ticks-wall-clock.md | open | 判据=计时源换单调基（Instant/quanta）或反向收口（注释改实时域 + 两处消费点加回拨防护），禁「注释说单调、实现是墙上钟」。dev 三态原样：`wedb/wbase/src/time.rs:41` 文档仍写「100ns 单调域，对标 Stopwatch.GetTimestamp」，:44-45 实现仍 `now_nanos()/NANOS_PER_TICK`（:33 `Clock::now_since_epoch()` 实时域）；消费侧无防护 `wedb/wnode/src/resp/metrics_commands.rs:109` `let elapsed = now_ticks - self.slow_log_start_ticks`（裸减回绕）、`wedb/wnode/src/resp/resp_server_session.rs:1094` 与 :1110 `as u64` 直转 | 立刻可派（wbase/src/time.rs + wnode/src/resp 两消费点，小棒）。分支 fix-now-stopwatch 名为本票但 tip 早已指向无关提交（b3071d31），无可复用载荷

task/ing/pending-lat-no-timing-site.md | 作废（后判 done，见文末同名条）| 原判 open/dev 未动

task/ing/session-metrics-option-dead-track.md | done | 判据=删会话选项侧布尔死轨、会话指标收敛为装配单点。`wedb/wnode/src/resp/resp_server_session.rs` 的 `RespServerSessionOptions` 结构体已无 metrics_sampling_frequency 字段（现字段仅 max_databases/enable_debug_command/latency_monitor/command_stats_monitor/lua*/aof*）；构造期改 `session_metrics: None`（:484-486，注释指「唯一创建点为 resp_session_consumer.rs:attach_session_metrics」），:296-298 字段文档同步；全仓 grep metrics_sampling_frequency 只剩 u64 真实轨（`wnode/src/service.rs:953/1307/1378/1622`、`wnode/src/server.rs`、wconf NodeOptions）| 删档（sess-metrics 已合 dev 2d20499d）

task/ing/zero-consumer-surfaces-batch-two.md | open | 八项死面 dev 全在场：`wacl/src/user_handle.rs:39 try_set_user`（且 `wnode/src/resp/acl_commands.rs:735` 仍无条件 set_user_handle，票明令改 CAS 重试环「删死口不成」）；`wmetric/src/info/garnet_info_metrics.rs:1223 get_info_metrics`；`wresp/src/catalog/data_provider.rs:65 try_export_resp_commands_data`；`wnode/src/storage/session/common/array_key_iteration_functions.rs:251 iterate_store`；`wnode/src/resp/basic_commands/mod.rs:89/108/394` network_ping/asking/echo 死方法；`wnode/src/aof/garnet_log/commit.rs:92 initialize_if` | 可派，但**先纠偏再派**：分支 zero-consumer-b2 的 6 个提交按「全删」口径写（含 a70b1922 删 try_set_user），与本票第四项修法（接 CAS 环、不得删）相反，直接合入会造新缺口。建议按票重开一棒、分域拆两票（wnode 分派臂单源 / 跨 crate 零消费删除）

task/ing/boot-assembly-projection-single-source.md | 作废（后判 done，见文末同名条）| 原判 open/载荷在未合分支

task/ing/client-pump-buffer-pool-unwired.md | open | 判据=客户端读泵改借 network_pool 缓冲、用完回池，消除 pump.rs 三处 `vec![0u8; READ_CHUNK]`。dev 原样：`wedb/wconn/src/network/pump.rs:200`、:235、:257 三处裸分配仍在；全仓池借用消费点只有服务端一处 `wedb/wnode/src/net/handler/drive.rs:81 buffer_pool.get_ref(0)`，wedb 自持两池（replication_manager/migration_manager 的 network_pool）get/get_ref 消费仍零 | 立刻可派（wconn/src/network/pump.rs + wedb/src/server/replication/replication_manager.rs 池下传）；与 /tmp/fork 的 b5-carry-wconn-wvector.patch 同 crate 不同面，注意 wconn 上在途改动

task/ing/snapshot-chunk-pooled-borrow.md | open | 判据=read_next_chunk 由 `Result<Vec<u8>,_>` 改池化借用直发。dev 原样：`wedb/wedb/src/server/replication/snapshot_transmission.rs:281` trait 签名仍 Vec<u8>、:306/:362 两实现同形、:314 `buf[..].to_vec()` 把池化 AlignedBuf 拷回堆、:365 `read_exact_at(vec![0u8; want], offset)` 零初始化新分配 | 立刻可派（wedb/src/server/replication/snapshot_transmission.rs 单文件 + wdev 池借用），可与 client-pump-buffer-pool-unwired 并一棒（同一「建池不借」病灶，跨 wconn/wedb 两面）

task/ing/replication-send-path-byte-cap.md | open | 判据=发送链补字节维度封顶（溢流队列/命令通道/写泵拼批三层）。dev 三层仍纯条数：`wedb/wedb/src/server/replication/replica_wire.rs:329 MAX_OVERFLOW_ENTRIES = 10_000`、:493 唯一超限判据 `self.overflow.len() >= MAX_OVERFLOW_ENTRIES`；`wconn/src/types.rs:12 CHANNEL_CAP = 1024`；全仓 grep 无 byte_cap/max_unflushed/RETRY_LATER 面（仅 snapshot_transmission.rs:95/:104 的 flushed_until 水位，与此无关）；`wconn/src/network/pump.rs:118-136` 拼批循环仍无字节分片 | 立刻可派（高优先，wconn/src/network/pump.rs + session.rs + wedb replica_wire.rs）；与已合 flush-bound 分支不冲突（那次只动刷写边界，未建字节顶）

task/ing/primary-checkpoint-cluster-callback.md | open | 判据=检查点内核按形态二分：集群走 on_checkpoint_initiated + add_new_checkpoint_entry 带钉线安全截断，单机才 TruncateUntil+Commit。dev 仍无条件单机链：`wedb/wnode/src/database/database_manager_base.rs:243` 拍后直接 `aof.truncate_until_async(&covered)`，本文件 grep is_primary/is_replica 零命中（covered 恒 tail）；`on_checkpoint_initiated` 生产调用点仍为零（只有 trait 声明 `wedb/wedb/src/server/cluster/i_cluster_provider.rs:13` 与 impl `cluster_provider.rs:1257`）；`add_new_checkpoint_entry` 生产唯一入口仍 `cluster_provider.rs:1086`（take_on_demand_checkpoint，副本 attach 链）| 立刻可派（wnode/src/database/database_manager_base.rs + wedb/src/server/cluster_provider.rs 接线），与 lastsave-dbid-routing 分支（未合，同文件 resp/garnet_api/slow.rs）有轻微同域，建议先合后者

task/ing/replica-wire-test-callback-in-production.md | open | 判据=AofSyncWire 收成单形（仅 Tcp）、FrameSink/CallbackWire 整体搬出产线（测试支撑 crate 或 cfg 门）、:37-38 两条 use 从生产视图消失。dev 原样：`wedb/wedb/src/server/replication/replica_wire.rs:56 enum FrameSink`、:139 `pub struct CallbackWire`、:613 `Callback(Arc<CallbackWire>)` 双臂与 :636/:651/:667/:674 逐方法 match 全在场 | 立刻可派（wedb/src/server/replication/replica_wire.rs + 三个 tests 改引新落点）；建议与 snapshot-chunk-pooled-borrow、replication-send-path-byte-cap 拆成两棒（同文件 replica_wire.rs 会撞车）

task/ing/cluster-slot-parse-error-parity.md（见上）

task/ing/del-unlink-vector-registry-slow-arm.md | open | 判据=向量集清退下沉到存储删除单点（或给 slow::del 加 vector 形参并在分派点接线），两臂同口径。dev 原样：`wedb/wnode/src/resp/array_commands.rs:176` 主删 + :186 `if !deleted && vector.is_some_and(|vm| vm.delete_vector_set(..))` 仍是快臂独有；分派点 `wedb/wnode/src/resp/garnet_api/slow.rs:599-601` 仍 `array_cmds::slow::del(&storage, &refs, &mut output)` 三参不传 vector；全仓 delete_vector_set 消费者无存储删除单点（其余命中均 keys.rs/raw.rs/bitmap 面）| 立刻可派（wnode/src/resp/array_commands.rs + garnet_api/slow.rs）。注意：fix-del-unlink-slowarm 分支 tip=7be9c56b 为纯文档提交，**无载荷**，不可当半成品复用

task/ing/error-frame-sanitize-bypass-trio.md | open | 判据=三处旁路收口，grep `extend_from_slice(b"-ERR` 与 `write_direct(b"-ERR` 在 wmetric/wlua 生产面归零 + 补 CRLF 注入回归用例。dev 三处全在场：`wmetric/src/latency/resp_latency_commands.rs:45`、:72、`wmetric/src/info/info_command.rs:66`、`wlua/src/runner/executor.rs:422` write_direct | 立刻可派（wmetric 两处 + wlua 一处，或净化下沉 RespWriter 单口）。分支 fix-err-frame-sanitize / dev-sanitize / error-frame-sanitize-close 虽在 merged 列表但无本題提交（git log --grep=sanitize 零命中），不可复用；与 task/done/resp-null-protocol-single-source.md 不同题

task/ing/gate-anchor-drift-reclean.md | stale-doc（半落地未记账）| 现档自述「虚构锚点 1 处在场 + 实测 bun js/check.js 退出码 1」已与代码事实矛盾：`wedb/wnode/src/resp/objects/tiered_collection_ops.rs:88-91` 已不挂 HashObjectImpl.cs:Set，改述「该符号锚点 1:1 挂在 wcol::hash::hash_object_impl::HashObject::hash_set，本判据位不复挂」，全仓 grep `HashObjectImpl.cs:Set` 零命中；19 族登记亦已大面积落盘（js/check/ignore/client.yml=LightEpoch、common.yml=DoubleTurnstileBarrier、storage.yml=Allocator 五件套·OverflowBucketLockTable·ProtectionTests·ScanIteratorEpochFailureTests、garnet/libs/cluster/Server/Replication/PrimaryOps/AofOperations/AofSyncDriver.yml）。唯一残留：`PrimarySync` 在 js/check/ignore 全域 grep 零命中。分支 gate-anchor-drift-reclean / salvage-gate 已合 dev（b054f1ee）| 建议主代理改写本票为「只剩 PrimarySync 一族 + 实跑 check.js 取退出码」的复绿小票，或直接归档后另立一薄票；不可按现档重派全量（会重复搬运已完成工作）

task/ing/cluster-provider-file-split.md（见上）

task/ing/acl-setuser-live-connection-propagation.md | open | 判据=ACL SETUSER 对目标用户全部在途会话即时生效（改全局共享句柄 CAS 换新，或鉴权侧版本比较/回读）。dev 三态原样：`wedb/wnode/src/resp/acl_commands.rs:289 apply_set_user` 仍只「点查→复制改写→回写 AclStore」，无任何会话通知面；全仓 `set_user_handle` 生产写点仅 :735 一处（发起连接自身）；`wedb/wnode/src/resp/resp_server_session.rs` acl_permits 仍只 `self.acl_user_handle.as_ref()..load().can_access_command(cmd)`，无存储回读/版本比较 | 立刻可派（wnode/src/resp/acl_commands.rs + resp_server_session.rs + wacl/UserHandle）；与 zero-consumer-surfaces-batch-two 第四项（同一 CAS 环，`wacl/src/user_handle.rs:39 try_set_user` 零消费）同病灶，建议并成一棒，禁两票各改一半

task/ing/lua-call-pending-suspend-handoff.md | open | 判据=RespScriptingApi::dispatch_resp 重入循环承接慢路径与阻塞两态挂起。dev 原样：`wedb/wnode/src/resp/resp_server_session.rs` 重入面仍只有 :2742 `if !session.take_output_watermark_yield()` 单一续消费信号，`take_blocked_wait`(:722)/`take_slow_wait`(:728) 在脚本面零调用 | 立刻可派（wnode/src/resp/resp_server_session.rs 脚本面）；/tmp/fork 有 ulua-audit-2026、ulua-audit-r2 两个在途工作树（wlua 审计），开工前确认让位

task/ing/claim-dead-b6.md | open | 三条判据全未落地：①`wedb/wlua/src/timeout.rs:164 pub(crate) fn active_deadlines` 仍在，读点仅 `wlua/src/cache.rs:405/:411` 测试；②`wedb/wnode/src/aof/replaycoordinator/aof_replay_context.rs:64 pub fn iter_keys_to_lock` 仍在，唯一读点同文件 :215 测试；③同簇 MemoryLogger 面无装配亦未判删：`wedb/wnode/src/logging.rs:149 MemoryLogger`/Provider/memory_loggers 在场，生产侧唯一引用是 `wnode/src/lib.rs:51` 再导出 | 可派（三处独立小删，一棒够）；注意本票与 zero-consumer-dead-surfaces-batch-six（next/）同域，主代理若已派批六即让位合并

task/ing/gate-anchor-drift-reclean.md（见上，partial）

task/ing/replica-attach-recovery-lock-window.md | open | 判据=恢复锁窗口握到 attach 体 finally（try_add_replica_async 成功路径不释放）。dev 仍提前释放：`wedb/wedb/src/server/cluster_manager_worker_state.rs:181` `rm.end_recovery(RecoveryStatus::NoRecovery, false)` 落在 try_add_replica_async（:126 起）尾段；`RecoveryStatus::CheckpointRecoveredAtReplica` 只在 `replica_diskbased_sync.rs:176` / `replica_diskless_sync.rs:206` 与 `replication_manager.rs:486` 的 cannot_stream_aof 判定里出现，attach 主链在 :181 已把状态打回 NoRecovery，故 :478 is_recovering 在整个传送/置换窗口恒假 | 立刻可派（高优先，wedb/src/server/cluster_manager_worker_state.rs + replication/assembly.rs 的 end_recovery 配对）；派前核对 next→ing 新到的同名片段（本票正文自述有同题壳，先剪壳）

task/ing/replica-replay-checkpoint-end-arm.md | open | 判据=CheckpointEndCommit 臂补「主端新版本 → 本地拍检查点 → 回调截断」链。dev 原样：`wedb/wnode/src/aof/aof_processor.rs` 的 `AofEntryType::CheckpointEndCommit` 臂体内仍只有 set_in_fuzzy_region(false) + process_fuzzy_region_operations + clear_fuzzy_region_buffer 三件事，无 store_version 比较、无拍检查点调用 | 立刻可派（wnode/src/aof/aof_processor.rs 触发环，上下游已在位：replica_replay_task.rs 记账 + cluster_provider.rs:1276 add_new_checkpoint_entry）；与 primary-checkpoint-cluster-callback 同链（后者接 on_checkpoint_initiated），建议并一棒或先派后者

task/ing/replication-comment-anchor-drift.md | open | 判据=模块头假文件名改真 + 三处 m4-checkpoint-import 引用清掉。dev 四处命中全在：`wedb/wedb/src/server/replication/snapshot_transmission.rs:5` 仍写 `TsavoriteSnapshotReader.cs`（garnet 实名 TsavoriteCheckpointReader.cs）、:8 与 `replica_sync_session.rs:124`、`replica_diskbased_sync.rs:65` 仍引不存在的 `task/ing/m4-checkpoint-import.md` | 极轻棒（纯注释/文档锚，可与 gate-anchor-drift-reclean 补登棒并成一棒，同属注释追溯链）

task/ing/replication-send-path-byte-cap.md（见上，open）

task/ing/wnode-service-open-node-zero-consumer.md | open | 判据=删 `wnode::service::open_node` 缺省装配薄壳，收敛为 `StorageSessionProvider::open` 单入口。dev 原样：`wedb/wnode/src/service.rs:753 pub fn open_node(data_path: impl AsRef<Path>)`，全仓 src/ 视图零调用（其余命中皆为 tests/ 本地同名 helper），`wnode/src/lib.rs` 亦只再导出 open_node_with_config | 极轻棒（单删 + 可能的 tests 改引），可与 zero-consumer-surfaces-batch-two 并棒（同为零消费面收口，不同文件）

task/ing/info-resetstat-gossip-reviv-reset-arms.md | open | 判据=INFO RESETSTAT 消费链补 gossip 与 reviv 两臂。dev 原样：`reset_gossip_stats` 生产调用点为零（只有 `wedb/wnode/src/cluster_provider.rs:135` 空默认实现、:271 trait 转发、`wedb/wedb/src/server/cluster_provider.rs:1657` 真实现），消费面 `wmetric/src/garnet_server_monitor.rs` 的 CleanupGlobalStats 对位段不读它；reviv 臂 `wedb/wnode/src/database/single_database_manager.rs:166-168` 仍是空函数体 + 注释「wkv 无复活化统计面」，且该口自身也无生产调用 | 立刻可派（wmetric/src/garnet_server_monitor.rs + wnode/src/database + wedb cluster_provider 三面接线）；与 reviv-knobs-zero-production-wiring（wkv 域，另一代理）在 wconf/wreviv 面上有轻度相邻，先说明边界

task/ing/checkpoint-dir-retention-wiring.md | open | 判据=检查点目录保留/清理接线（wcpr purge_outdated/purge_all/purge_checkpoint 上生产链）。dev 原样：全仓 grep `purge_outdated` 在 wcpr 之外零命中（定义与导出仍只在 `wedb/wcpr/src/manager/mod.rs`、`wcpr/src/lib.rs`）| 立刻可派（wnode/src/database/database_manager_base.rs 检查点完成尾段 + wedb/src/server/replication/checkpoint_store.rs），与 primary-checkpoint-cluster-callback 同一收尾段，建议同棒或串行

task/ing/object-output-payload-direct-write.md | open | 判据=ObjectOutput 载荷改挂载会话输出尾段单步直写、去中转 Vec。dev 原样：`wedb/wcol/src/resp/output.rs` 的 `pub struct ObjectOutput { pub payload: Vec<u8>, pub result1: i64, pub output_flags }` 仍是自带中转向量（result2 已由 task/done 那票删掉，不属本票）| 立刻可派（wcol/src/resp/output.rs + wnode 20 处构造点），是 f61-obj-output 分支未合的续题，开工前确认该分支无残留载荷

task/ing/object-scan-coscan-kernel-single-source.md | open | 判据=同步段与慢路径的纯校验四段单源、慢段 cmd_name 补 All 臂。dev 原样漂移仍在：`wedb/wnode/src/resp/objects/shared_object_commands.rs:45` 同步段有 `GarnetObjectType::All => "COSCAN"`，慢段 :369-374 却是 `Hash/Set/SortedSet` 三角 + `_ => "NONE"`（All 落 NONE），sub_id :386-390 同样 `_ => 0`，:335 注释仍自称「与同步段单源同口径」 | 立刻可派（同文件收口，纯 wnode/src/resp/objects），与 object-slow-dispatch-arg-reparse-single-source 同域，宜并一棒

task/ing/object-slow-dispatch-arg-reparse-single-source.md | open | 判据=对象族四慢分派的参数推导与缺失态应答单源（消除快/慢两份），并修 SINTERCARD 慢侧半开区间漂移。dev 原样：慢侧重推导注释群仍在（`wnode/src/resp/objects/list_commands/slow.rs:559`、:665、`hash_commands.rs:828`、:858、:1045、`sorted_set_commands/slow.rs:162`、:190、:457），谓词漂移两处并存：`wnode/src/resp/objects/set_commands.rs:379` 半开区间 `(0..=i32::MAX)` 与 :448/:1081 全区间 `(i32::MIN..=i32::MAX)` | 立刻可派（wnode/src/resp/objects/*，与上一票同文件域，合并成一棒更省）

task/ing/qcode10-parse-db-index-i32-parity.md | done | 判据=DB 号解析改 int32 域、超 i32 落「非整数」档。dev 已成立：`wedb/wbase/src/num.rs:174 pub fn parse_db_index(raw: &[u8]) -> Result<i32, DbIndexError>`，:175-179 `strict_i32` → None=NotInteger、负数=OutOfRange，文档 :166-169 已按 int32 值域改写；消费点三处齐用（`wnode/src/resp/admin_commands.rs:626`、`wnode/src/resp/garnet_api/slow.rs:579`、`wnode/src/resp/txn_resp_commands.rs:296`）；用例已钉新口径（`wnode/tests/resp_tests.rs` can_select_command 断 2147483648/3000000000/-2147483649 → value is not an integer）| 删档（qcode10-parse-db-i32 已合 dev 7b171efc）

task/ing/boot-assembly-projection-single-source.md | done（本轮盘点期间合入）| `wedb/wedb/src/server/boot.rs:28-40`（dev 6aea5715）已删三局部量与逐字段 setter，注释改指「采样节拍/延迟监视/逐命令统计/TLS 由 ServerBootstrap::run_async 一处从 NodeArgs 投影」；同批 `wconf/src/node_options.rs`、`wnode/src/server.rs`(-148 行)、`wnode/src/service.rs` 收口 | 删档（merge 6aea5715）

task/ing/pending-lat-no-timing-site.md | done（本轮盘点期间合入）| `wedb/wnode/src/storage/session/storage_session.rs:151-166` with_pending_metrics 已起停 `LatencyMetricsType::PendingLat`（:162 start / :164 stop，latency 为 None 时早返不记），新用例 `wedb/wnode/tests/pending_latency_timing.rs:96` 断 PENDING_LAT 桶长度；ignore 面 `js/check/ignore/garnet/libs/server/Storage/Session/Metrics.yml` 亦在合入清单 | 删档（merge 273b49ee；盘点期间落地，此前判 open 作废）

task/ing/scan-type-exact-case-forms.md | open | 判据=SCAN TYPE 值改精确两形态比对（混合大小写走未知类型臂）。dev 原样：`wedb/wnode/src/resp/array_commands.rs:116-127` 五类型仍 `type_arg.eq_ignore_ascii_case(b"zset"/"list"/"set"/"hash"/"string")`，:66 注释仍自述「使用 eq_ignore_ascii_case 支持混合大小写」| 极轻棒（wnode/src/resp/array_commands.rs parse_scan_filter + 同文件 :708/:734/:738 单测改口径），可与 cmd-strings-input-token-single-source 并棒（同为 array_commands 输入面）

task/ing/cmd-strings-input-token-single-source.md | open | 判据=解析期输入 token 收口 wresp 单点。dev 原样：`grep 'b"COUNT"'` 生产位点仍 19 处散内联（wcol/src/types/scan_input.rs、wcol/src/list/list_object_impl.rs 的 `== b"COUNT" || == b"count"` 双写形态、wnode 多处），`wresp/src/cmd_strings.rs` 无成组输入 token 常量（grep `pub const COUNT`/token 组零命中）| 可派（跨 wresp/wcol/wnode/wlua 的机械收口，量大但零风险）；注意与 scan-type、msetnx 两票同改 array_commands.rs，串行为佳

task/ing/slow-path-string-key-admin-arms.md | open | 判据=字符串族与键管理族补慢分派臂，消除 `_ => RESP_ERR_ASYNC_REQUIRED` 兜底。dev 原样：`wedb/wnode/src/resp/garnet_api/slow.rs:827` 仍 `_ => write_error_raw(&mut output, RESP_ERR_ASYNC_REQUIRED)`，文件 1097 行，字符串族（SetEx/Getex/Ttl/Expire 等）未入表 | 立刻可派（wnode/src/resp/garnet_api/slow.rs + array_commands/basic_commands 慢实现），本票是客户端可见错误，优先级高；与 lastsave-dbid-routing 分支（未合，同文件 slow.rs）撞车，先合该分支

task/ing/msetnx-slow-path-meta-domain-probe.md | open | 判据=MSETNX 慢路径存活判定补 Meta 域探针（与快路径 probe_alive_with_prefix 三域同口径）。dev 原样：`wedb/wnode/src/resp/garnet_api/slow.rs` 的 `C::Msetnx` 臂仍只 `read_tag_with(key, KeyTag::String, ..)` + `KeyTag::ObjectEnvelope` 两探，无 `KeyTag::Meta` 臂 | 立刻可派（单点补域 + 一条升阶冷键回归用例）；与 del-unlink-vector-registry-slow-arm 同为「慢臂漏面」，可并一棒（同文件 garnet_api/slow.rs）

task/ing/tiered-collection-ops-file-split.md | open（并恶化）| 判据=tiered_collection_ops.rs 按 C# ObjectStore 会话分界纯搬运拆分。dev 现态：`wedb/wnode/src/resp/objects/tiered_collection_ops.rs` 已涨到 2305 行（票载 2167），`wnode/src/resp/objects/` 下无 tiered_* 子模块目录承接 | 可派（纯搬运），但主代理注意：task/reject 已对同型「纯搬家分文件」主张判否两次（resp-server-session-file-split、wnode-service-split），本票与二者不同（单文件确有四执行体分界），若政策收紧可先复核再派

task/ing/garnet-scan-cs-corpus-parse-gate.md | open（非我域，顺手判）| 判据=check.js 对 C# 语料解析失败须硬失败/告警。dev 原样：`js/check/*.js` grep `hasError`、`corpus_invalid` 零命中，`js/check/garnetScan.js:5` 仍直用 treeSitter csParser 无失效判定 | 建议转交工具链域代理（属 js/check 而非 crate 域），与 gate-anchor-drift-reclean 的补登棒同批开工最省

task/ing/acl-load-save-placeholder-ok.md | open | 判据=ACL LOAD/SAVE 不再为「什么都没做」回 +OK（对位 C# ValidateACLFileUse → RESP_ERR_ACL_AUTH_FILE_DISABLED）。dev 原样：`wedb/wnode/src/resp/acl_commands.rs:440 network_acl_load` 函数体仍 :450 `write_raw(output, cs::RESP_OK)`；全仓 grep `ACL_AUTH_FILE_DISABLED` 零命中（常量未移植）| 轻棒可派（wacl/wresp/wacl 文案），与 acl-setuser-live-connection-propagation 同文件域（acl_commands.rs）→ 建议并一棒

task/ing/custom-obj-multikey-metagate-and-tag-u8.md | open | 判据=多键扩展命令知识入静态清单 + 标签型别不擦 u8。dev 原样：`wedb/wnode/src/resp/resp_server_session.rs:1956` 仍 `if custom.name.eq_ignore_ascii_case("JSON.MGET")` 运行时按名特判，:224 `pub object_tag: u8`；`wedb/wcustom/src/object_desc.rs:22-30` CustomCommandMeta 仍只 name/command_type/arity/fns（无多键位、无 tag 型别透传）| 可派（wcustom + wnode + wcol 三面），优先级低；无在跑分支撞车

task/ing/session-parse-state-extensions-bypass.md | open | 判据=13 个 (parse_state, buf, idx) 形态口要么接线要么删，且消除与 wbitmap 的复抄双挂锚点。dev 原样：`wedb/wnode/src/session_parse_state_extensions.rs` 仍 17 个 `fn try_get_*`，全仓 src/ 视图零消费者（`wmetric/src/info/info_command.rs:108` 与 `wresp/src/options.rs:273` 命中均为各自同名异实函数）| **勿重派**：工作树 /tmp/fork/fix-session-parse-state-bypass（HEAD=dev tip）正在做本题，主代理先收那一棒

task/ing/testhook-and-bypass-parser-cfg-gate.md | open | 判据=TestHook 五口与两口旁路解析器加 cfg/可见性门。dev 原样：`wedb/wepoch/src/epoch.rs:721 pub fn test_hook_this_thread_entry`（同族另四口同形，无 cfg）、:187 active_instance_count、:196 reset_all_instances 皆裸 pub；`wedb/wnode/src/resp/parser/resp_command.rs:409/:425` 两口仍 pub 无门（唯一非 tests 消费者是 `wlua/src/runner/host.rs:122` 的 vtable 形参，属旁路面）| **勿重派**：/tmp/fork/fix-min-page-guard 与 fix-session-parse-state-bypass 在途；本票与二者同批（qw.design 拆出），建议排在两者之后

task/ing/wconf-min-page-size-guard-unwired.md | open | 判据=MIN_PAGE_SIZE_BYTES 有真实校验读者（对位 C# ValidateAndConvertPageSize）。dev 原样：`wedb/wconf/src/size.rs:14 pub const MIN_PAGE_SIZE_BYTES: i64 = 512;` 全仓仅 `wconf/tests/garnet_server_config_tests.rs:12/:221` 两处自证 | **勿重派**：/tmp/fork/fix-min-page-guard 正在做（分支 tip=dev bfb3cecd），等收

task/ing/readme-crate-map-drift.md | open | 判据=README 家族去掉幽灵 wram / 虚构 16 分片 / ext_* 假名并再生成 mdt。dev 原样：`readme/zh/README.md:47` 仍有 mermaid 节点 `wram["wram: 内存分配器与内存水位追踪"]`、:91 对标矩阵行列 wram、:106 仍称 wnode 含「16 分片无争用网络缓冲池」；根 `README.md`（121 B）与 `wedb/README.md`（48 KB）并存未收口 | 可派（纯文档，风险低）；分支 readme-crate-map-drift / salvage-readme / salvage-readme2 均已合 dev 却未清这些串，属陈旧壳不可复用。注：本票跨全 crate，若与并行代理重复请按「文档域唯一持有」裁定

task/ing/zero-consumer-dead-surfaces-batch-six.md | open | 判据=24 个零消费 pub 面逐条判「接线 / 删 / cfg」。抽查全在场：`wedb/wnode/src/rangeindex/range_index_manager_replication.rs:106 STREAMED_PUBLISH_LOG_ARG`、:178 set_aof_stream_chunk_size、:218 replicate_range_index_del；`wedb/wnode/src/aof/aof_backpressure.rs:229 set_counter_log`、:237 get_shipped_watermark | **勿重派**：/tmp/fork/fix-dead-batch6 工作树正在做（分支 tip=dev bfb3cecd）；落地时注意票内三处联动裁定（from_der 保留、with_revivifiable_fraction 让给复活池票、attach_monitor 与已合的 pending-lat 联动）

## 未盘（判为并行代理域，避免越界）
aof-replay-virtual-domain-context、aof-store-rmw-dead-replay-arms、checkpoint-flush-throttle-knob、json-commands-file-split-anchor-decl(wext_json)、range-index-replication-shutdown-dispose(wbftree/wkv)、reviv-knobs-zero-production-wiring(wkv)、rmw-atomic-read-modify-write-window(wkv)、string-rmw-key-bucket-lock(wkv)、tiered-drain-envelope-tombstone(wkv)、tiered-reflush-atomic-swap(wkv)、windex-prefetch-window-dual-impl(windex)、wreviv-test-record-alignment-second-source(wrecord)、wtxn-lock-stripe-count-parity(wtxn)、zset-aggregate-member-ttl-filter(wcol)、zset-o1-bypass-expiry-count(wcol)
