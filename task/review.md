请一个一个查看./garnet 从开始到现在的每一次提交

然后，开子代理，结合 diff 的改动 和 ./sh/skills/rust_review/SKILL.md 审查、优化代码

diff 只是思考我们代码改进的线索

实现参考的代码还是以最新版本的./garnet 为准

让子代理运行 ./fork.sh 开 worktree 到/tmp/fork/下面，优化，写完、测试之后合并到当前目录，清理 worktree。

如果遇到主分支修改，请提交，然后合并（优化一次，合并一次、同步主库到 worktree，然后再继续下一次循环）。

合并之前，先跑通测试

实现对标 garnet，但是不需兼容（.agents/skills/transpile/SKILL.md 中描述了我们的修订优化细节）

序列化用 bitcode，尽可用二进制优化，不需要向下兼容

一处定义，清理重复冗余代码，不写别名（简化复杂类型定义除外）

让代码简洁、优雅、清晰，一个函数只有一种调用方式（禁止重复定义）

参考./garnet/，严禁有多套机制（避免 ai 制造冗余垃圾），反复审查，避免重复的代码、机制，避免过度设计

数据链路要打通，对标 c#的调用流程，删除没用的函数，测试、调试用的函数用 cfg（比如 cfg test bench 等等）

严禁用 hack 的方式编程，除了 .agents/skills/transpile/SKILL.md 写明的差异，其他实现严格对标 c#

代码基于 compio 生态，一个线程一个 cpu，要利用这一点优化

如果重复代码、常量跨模块，并且两个模块不应该有依赖关系；那么拆分到 wedb/wbase 的 cfg 模块，作为特性启用，然后，按需添加 wbase 的特性依赖

如果开子代理对代码有修改，继续开新的子代理，反复审查、优化代码，循环，直到子代理认为代码已经达到生产级别标准