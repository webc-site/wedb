开子代理，按 ./sh/skills/rust_review/SKILL.md 审查、优化代码

让子代理开 worktree 到/tmp/fork/下面，优化，写完、测试之后合并到当前目录，清理 worktree。如果遇到主分支修改，请提交，然后合并（优化一次，合并一次、删除之前的 worktree，重开新的）。

合并之前，先跑通测试

实现对标 garnet，但是不需兼容

序列化用 bitcode，尽可用二进制优化，不需要向下兼容

一处定义，清理重复冗余代码，不写别名（简化复杂类型定义除外）

让代码简洁、优雅、清晰，一个函数只有一种调用方式

代码是 compio 生态，一个线程一个 cpu，要利用这一点优化

如果重复代码、常量跨模块，并且两个模块不应该有依赖关系；那么拆分到 embed/wbase 的 cfg 模块，作为特性启用，然后，按需添加 wbase 的特性依赖

如果开子代理对代码有修改，继续开新的子代理，反复审查、优化代码，循环，直到子代理认为代码已经达到生产级别标准