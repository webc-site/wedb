登记档（zcode-r91-triage-leakproc 裁定）：本票为持续纪律规范非待执行任务，不入 todo 池。消费方式：各会话提交行为自律遵循四则；追认注记挂 task/zcode.review-plan.md 对应轮次条目；不符笔数统计、出生红零新增、门禁行巡检三项并入后续 churnreview/recheck 档固有检查面。

提交 message 面与实触面失真、test 与 style 之名夹带业务改动、审查档被并发提交顺带走档、出生红红窗容忍（提交流纪律四则治理，纯流程票）

票别：流程纪律治理票（无业务码修改面，r88-recheck87 预告、r89-recheck88b 确认触发条件达成后 r90 轮立项）
触发链：zcode-r88-recheck87 三.1「同型第二例……若第三例再现宜入流程纪律票」→ bf76f9ee 第三例落库 → zcode-r89-recheck88b 二.4 确认「触发条件本轮达成，建议下轮立流程纪律票（提交流与审查档分离）」。

问题分析：
1 Garnet 契约对齐（本票契约对象是仓库自身流程纪律而非 C# 原型）。其一，AGENTS.md 开发后条款：「如修改了代码，请运行 ./test.sh 测试（没有修改代码不需要运行）」——业务与测试改动入库前测试义务明文在册，出生红三例即该义务未执行。其二，task/fix.md 第 5 节主代理集成门禁：子代理合并到主目录后由主代理统一运行 ./test.sh 与 ./sh/clippy.sh，遇红开新 issue 重走闭环；第 2 节子代理禁跑 test.sh/clippy.sh（防多代理互锁）与之互为表里——全量门禁归主代理独占，任何业务提交不得顺手改动 wedb/test.sh 门禁行（78be682a 已违反并闭环，见 2 末段）。其三，task/review.md 审查档自有入档通道：review(zcode) 纯档提交先例（ba08ae5b、454697b3、52763037、2cfc9f08）message 与实触面一致，审查档不缺提交通道。其四，commit message 覆盖实触面是 git 使用基本约定，仓内正例 f9c1a065（docs: track task issue files，纯档入库）被历轮记录为「message 与实触一致，非夹带」，即判定基准已在案。
2 工程现状确证（时间序证据链，七笔哈希均 git show 亲证，2026-09-24 同日）。夹带审查档三例：其一 d7acd33b（12:53:54，test(wnode): 修正 resp_sorted_set 中 WRONGTYPE 帧断言）实触四文件，除 wedb/wnode/tests/resp_sorted_set.rs 六行外夹带三张 r86 审查档（task/issue/zcode-r86-churnreview.md 45 行、zcode-r86-devsync3.md 79 行、zcode-r86-draft-remnant.md 46 行，174 插入中 170 行为档）。其二 c767817c（13:06:39，test(wnode): use seed_ttl_on_key）实触六文件，除 wedb/wnode/tests/ttl_rmw_semantics.rs 八行外夹带五张 r87 档（zcode-r87-consumewatch2/devsync4-draft/orphan-rmw/recheck86/devsync4-deviations-corr，237 插入中 229 行为档）。其三 bf76f9ee（13:21:39，test(wnode): use real WedbVectorStoreCallbacks）实触八文件，除测试文件十八行外夹带七条 task 路径（r88 四记录档 consumewatch3/recheck87/triage-light2/triage-orphan、devsync4-deviations-corr issue→todo 改名、todo 两新档 wkv-orphan-rmw/wnode-aof-waitforcommit）。三例同型：test 提交带走并发审查会话暂存档，message 只述测试面。格式化或 test 之名夹带业务改动三例：其一 cf7be051（08:17:20，chore: 同步外部审查工单与代码格式化）message 预告了档同步与格式化，但夹带三处 src 语义面——wedb/wconf/src/node_options.rs endpoints() 重构最重（bind 解析由 unwrap_or_default().trim() 后空判分派改为 None/Some(s).trim() 直配，空与纯空白 --bind 由回落 DEFAULT_BIND/DEFAULT_BIND_ANY 变为空列表走注释自述的拒启臂，行为分叉非格式）；wedb/wdev/src/segmented_device/handle.rs sync_dir 臂改 let 链（控制流重写）；wedb/wedb/src/main.rs 错误域导入面改写。其二 c56d35a4（11:37:12，style: 格式化 wdev, aof 与对应测试）夹带 wedb/wcpr/src/manager/mod.rs 三处 !dir.exists() 改 !dir.is_dir()（checkpoint 目录判定，同名文件存在而非目录时行为由放行变短路返回，TOCTOU 面变化，非纯格式）。其三 466a29d3（11:46:47，test(waof): 适配恒分段设备下段文件盘面统计断言）夹带两处生产改写——wedb/wkv/src/session/raw/write/rmw.rs upsert_rmw 过期裁决门由 check_expired 改为持窗直清 purge_expired（免重复取闩自锁，并发语义变更）；wedb/wkv/src/ttl.rs probe_alive 整函数重构（check_expired 错误吞没 let _ =、锁争用跳过物理清除尽力而为语义），连带 wedb/wkv/src/compact.rs 与 bench/wkv_engine.rs。出生红三例（r87-recheck86 三.3 在案登记，同窗 07:41-12:53 同型）：8513ef5b（07:41:52，resp_admin 三断言出生即错，红窗 5h07m 至 a26d1ea4 修复）、c518dd0f（08:35:05，zset 断言无句号形态出生红，红窗 4h18m 至 d7acd33b 修复）、shutdown_second_signal（78be682a 前约 10h 红窗）——三例入库时全量 test.sh 均未跑或红被容忍。另 78be682a（11:23:55，fix(compact)）夹带 wedb/test.sh feature.check.sh 行注释禁用门禁与 shutdown_second_signal --log-level info 追加，经 zcode-r85-gateaudit 立案、866bcfff 门禁核验恢复（恢复注记当时有效；后 72aeb39 init 压扁重建前史、门禁行随重建静默丢失，「在位亲证」宣称失效——由工单 gate-featurecheck-wiring-lost 于本轮恢复接线并对齐现树，详见该票）与 zcode-r85-recheck84 夹带清单订正，已单独闭环恢复，本票不重复追责，仅作门禁不可顺手改的例证收录。死哈希注记：本档所引 78be682a、866bcfff 及历轮例证哈希均因前史于 72aeb39（init）压扁重建而不可达（git cat-file 报 not a valid object name），仅作文字史证留存，不作可核验锚。
3 逻辑危害确证。其一 message 失真致审查对账反复：同一笔提交被多轮重审——cf7be051 的 src 语义面曾在 r82-churnreview 按消息面记为纯格式触碰，后续轮复核才分离 endpoints 等分叉面；78be682a 夹带清单在 r84 两票均漏记、r85-recheck84 再订正；每轮 churnreview 对 message 与实触逐笔 git show 亲证，成本随失真笔数线性放大，且漏检即漏档。其二门禁绕过面：门禁行被业务提交顺手注释（78be682a）属最重形态——若非 gateaudit 独立核验，feature.check.sh 检查面静默失效且无任何流程痕迹。其三红窗容忍：三例出生红合计约 20 小时红窗，AGENTS.md 开发后测试义务未执行使红滞留库内，期间任何合入的基线可信度受损，后续归因须逐笔回溯出生点（r86-churnreview、r87-recheck86 已实际付出该成本）。其四根因注记与治理面收窄：多会话并发是常态（审查循环与消费会话共享 index），共享 index 无法原子隔离是夹带档的结构性根因，历史判例（ba1c1325、f3aafe53）已裁「内容无损勿做历史手术」——不对已发生夹带做 rebase 改写；故本票治理面收窄为两个可执行点：message 失真（可对拍可拒收）与纪律未执行（测试义务、门禁不可顺手改），档夹带本身以追认注记消化。

涉及代码：
rust 文件与函数：本票无业务码修改面，例证提交清单如下（哈希 + message 宣称面 vs 实触差面）。d7acd33b（宣称 test 修正，实触夹带三张 r86 档）；c767817c（宣称 test seed_ttl_on_key，实触夹带五张 r87 档）；bf76f9ee（宣称 test callbacks，实触夹带七条 task 路径）；cf7be051（宣称 chore 格式化，实触夹带 wedb/wconf/src/node_options.rs:endpoints、wedb/wdev/src/segmented_device/handle.rs:sync_dir 臂、wedb/wedb/src/main.rs 三处 src 面）；c56d35a4（宣称 style 格式化，实触夹带 wedb/wcpr/src/manager/mod.rs 三处 exists→is_dir）；466a29d3（宣称 test 断言适配，实触夹带 wedb/wkv/src/session/raw/write/rmw.rs:upsert_rmw、wedb/wkv/src/ttl.rs:probe_alive 两处生产改写）；78be682a（宣称 fix compact，实触夹带 wedb/test.sh:7 门禁禁用，已闭环恢复）。
对应 c# 文件与函数：N.A.（本票为纯流程治理票，无 C# 原型对应；node_options.rs endpoints 空列表拒启臂的 C# 语义参照 garnet Options.cs:796 仅为例证注脚，非本票修改面）。

精炼执行方案：
1 提交前 staged 面对拍：提交前以 git diff --cached --stat 逐路径与拟写 message 对拍，message 未预告的路径不入本笔；多会话共享 index 场景在暂存完成后到提交前一刻再核一次，发现 task/ 路径或他人 staged 档混入，以 git restore --staged <path> 剥离，留档主经 review(zcode) 通道提交。
2 提交分离四类：业务 src、测试 tests、格式化 style、审查档 task 四类分笔提交，一笔一义；审查档统一走 review(zcode) 纯档提交（先例 ba08ae5b、52763037、2cfc9f08），消费会话认领改名（todo→ing→done）随自身 task(ing) 提交或纯档提交，不搭业务笔。
3 红测试零容忍：凡改动业务码或测试码，入库前跑受影响包定向验证（cargo nextest run -p <pkg> 或加 --test <target>），红不入库、不留给主代理门禁兜底；全量 ./test.sh 与 ./sh/clippy.sh 仍归主代理门禁独占（task/fix.md 第 5 节），子代理不越权跑全量；wedb/test.sh 与 feature.check.sh 门禁行对一切业务提交禁改，门禁调整须独立流程票承载。
4 夹带追认义务：夹带已发生且内容无损时，不做历史手术（ba1c1325/f3aafe53 判例，禁 rebase 改写禁回退），由发现轮审查档在 task/zcode.review-plan.md 对应轮次条目下附追认注记，注记三要素为提交哈希、夹带面清单、内容无损判据（档全量入库无删改、或语义面经复核对齐），供后续轮对账免重复取证。
5 验证点：后续 churnreview 对账新增统计项「message 面与实触面不符笔数」，目标为零；出生红零新增（红窗计时口径：提交落库时刻至红被门禁或复核暴露时刻）；门禁行 wedb/test.sh:10-11（feature.check.sh 与 allow.check.sh 双接线，gate-featurecheck-wiring-lost 恢复＋allow 同挂兑现）常态在位（巡检项，行号随现树更新；原锚 :7 及 78be682a/866bcfff 系前史压扁（72aeb39 init 重建）后不可达死锚，已于 2 段改述）。
