梳理 ./next 下面各 ai 观点为正交审查，规划区分优先级，优先解决重要的问题(比如清理死代码，清理重复逻辑，清理多套架构，避免污染扩散)

然后， 并发开子代理，让子代理参考 ./task/refine.md 思考如何把 ./garnet 改造为 rust （注意，.agents/skills/transpile/SKILL.md 描述了对 garnet 的改进需求，要结合到一起思考、审查）

注意：告诉子代理，next 下面观点是 ai 生成，可能有问题，让子代理务必对照 c#代码和改进需求，甄别，思考，要勇于拒绝。

比如: 不要实现加载 c#模块，不要实现任何微软认证之类与微软平台绑定的东西

如子代理觉得意见不合理，删除相关行，整理到 ./task/reject/ 下面，写明拒绝的原因

反之，如观点有可取，也让子代理删除 ./next 下的相关行，在./task/ing/ 下创建 md 细化方案（不写表格、不加粗），然后运行 ./fork.sh 在 /tmp/fork 开分支，按照规划开发

开发之后，让子代理合并到主目录，并 mv ./task/ing/ 下面的文档到 ./task/done/

总之，不管是拒绝还是实现，都让子代理在任务开始之前，清理./next 下面相关行

一个子代理只处理一个问题，避免贪多嚼不烂

务必告诉子代理，不需要向下兼容，不需要旧版数据迁移，直接删除相关代码

让子代理在合并到到主目录之前，先对主目录做提交，然后合并主目录最新的提交（因为可能有其他子代理在并发开发，制造了冲突、垃圾、重复实现），解决冲突，按.agents/skills/rust_review/SKILL.md ，对代码审查，解决冲突、清理死代码，合并重复代码，确保只有一套机制，实现复杂度要对标 c#，而不是添加额外的复杂度

审查之后，子代理主动合并到主目录，删除相关 worktree

注意，告知子代理，只运行 cargo check，不运行 test.sh 和 ./sh/clippy.sh （避免相互阻塞）

test.sh 和 ./sh/clippy.sh 都由子代理完成合并到主目录后，由主智能体运行。

因为这两个操作都很慢，每次运行后，要检查是不是有新子代理(可能是多个)完成了任务，如果有，要再次运行。

如遇到问题，开新的子代理，让它运行./fork.sh，开分支，参考 c# 的实现，修复，清理之后再次按 .agents/skills/rust_review/SKILL.md 审查，清理死代码，重复代码，优化实现。

反复开新的子代理迭代，不断拒绝或实现，清理相关待办，直到 next 下面所有文件都被删除（如果文件没有待办了，删除文件）