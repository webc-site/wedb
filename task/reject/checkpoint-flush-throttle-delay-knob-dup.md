重复：task/ing/checkpoint-flush-throttle-knob.md（关键符号 ThrottleCheckpointFlushDelayMs/AsyncFlushPagesForSnapshot/flush_pages_range 命中）
优先级：中
检查点刷盘 IO 节流旋钮 checkpoint-throttle-delay 整链缺失：C# 页间等待+睡眠五级链齐备且默认启用，rust 检查点刷盘无任何节流面也无 ignore 登记
  具体问题：C# 为控检查点大刷盘对前台 p99 的干扰专门做了节流旋钮：配置面 checkpoint-throttle-delay（HelpText "-1 - disable throttling; >= 0 - run checkpoint flush in separate task, sleep for specified time after each WriteAsync"）→ GarnetServerOptions.CheckpointThrottleFlushDelayMs（默认 0）→ GarnetServer.CreateStore 注入 kvSettings.ThrottleCheckpointFlushDelayMs → AllocatorBase.AsyncFlushPagesForSnapshot 逐页刷盘，throttle >= 0 时每页 WaitOneFlush（等上一页完成再发下一页，流水线深度 1）+ Thread.Sleep(delay) 制造页间间隙 → 索引检查点快照刷盘同参数（IndexCheckpoint.BeginMainIndexCheckpoint）。即 C# 默认值 0 就是「串行等完成」节流形态，> 0 可加大间隙，-1 才全速连发。rust 侧该旋钮五级链整体不存在（wcpr/wconf 全域 grep throttle 零命中），检查点刷盘走 flush_all → PendingFlushList 贪心合并为单次巨型 write_aligned（一次提交全部页），大库快照期间设备队列被单次大写长时独占、无页间间隙可调，SAVE/BGSAVE/AOF 超限三条检查点生产链全部裸奔；js/check/ignore 亦无该旋钮的不移植登记（storage.yml:1055 只登记设备基类 Throttle/分片 TryComplete，不覆盖本旋钮）。与 task/ing/on-demand-checkpoint-flag-unwired.md 不同面（那张管 on-demand-checkpoint 开关恒 true，本条管刷盘 IO 节流旋钮）；与 next/glm.db.md 刷盘上界条不同面（那条管刷盘边界与在途编码竞态的正确性，本条管刷盘节奏对前台延迟的可观测调控）。修法：wconf 补 checkpoint-throttle-delay 槽位（-1 关闭 / >=0 启用），wcpr/whlog 刷盘循环按值插入页（或页批）间 sleep 间隙与等完成串行形态，或将该旋钮显式登记 ignore 并写明 rust 单次合并大 I/O 形态下不承接的理由。
  rust：wedb/wcpr/src/manager/create.rs:227 store.flush_all()（检查点刷盘唯一入口，无节流参数）；wedb/wkv/src/store/flush.rs:114 flush_all、:100-107 flush_pages_range（直接转发）；wedb/whlog/src/hlog/io.rs:230-289 flush_pages_range（PendingFlushList 合并 + 单次 flush_range_aligned 巨型写，无页间等待/睡眠面）；旋钮缺位 wedb/wconf/src/ 全域 grep checkpoint throttle 零命中；生产链 wedb/wnode/src/database/database_manager_base.rs:186-250 take_database_checkpoint_async、wnode/src/service.rs:516-535 spawn_aof_size_limit_task
  C#：libs/host/Configuration/Options.cs:416-418（[Option("checkpoint-throttle-delay")]，语义 -1/>=0 双态）；libs/server/Servers/GarnetServerOptions.cs:359（CheckpointThrottleFlushDelayMs = 0 默认）；libs/host/GarnetServer.cs:476-477（注释 "Run checkpoint on its own thread to control p99"，注入 kvSettings.ThrottleCheckpointFlushDelayMs）；libs/storage/Tsavorite/cs/src/core/Allocator/AllocatorBase.cs:2298-2322（AsyncFlushPagesForSnapshot：>= 0 转 Task.Run 独立任务 + FlushCompletionTracker(enableThrottling)）与 :2393-2398（每页 writeIssued 后 WaitOneFlush + Thread.Sleep(throttleCheckpointFlushDelayMs)）；libs/storage/Tsavorite/cs/src/core/Index/Recovery/IndexCheckpoint.cs:25（BeginMainIndexCheckpoint 传 ThrottleCheckpointFlushDelayMs，索引快照同节流）
