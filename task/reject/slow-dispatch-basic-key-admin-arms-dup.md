重复：task/ing/slow-path-string-key-admin-arms.md（关键符号 慢路径分派臂/asynchronous completion 哨兵 命中）
优先级：高

字符串与键管理族命令快路径降级后慢路径分派表无承接臂，冷键/环形页翻转触发即回 "ERR command requires asynchronous completion"
    SET/SETEX/PSETEX/SETNX/SETEXNX/GETSET/SETRANGE/GETRANGE/SUBSTR/STRLEN/GETEX/GETDEL/APPEND/INCR/DECR/INCRBY/DECRBY/INCRBYFLOAT/EXISTS/EXPIRE/PEXPIRE/EXPIREAT/PEXPIREAT/PERSIST/TTL/PTTL/EXPIRETIME/PEXPIRETIME/RENAME/RENAMENX/DUMP/RESTORE 及 OBJECT 四子命令在 raw::dispatch 快表执行，遇磁盘候选（probe_alive/read_user_sync/ttl_read_sync 返回 None/Deferred）或环形页翻转/TTL 清退降级信号（try_upsert_sync/put_ttl_sync 返回 Ok(Err)）时按约定返回 Ok(false) 挂 SlowWait，但 exec_slow_impl 分派表未接其中任何一臂（同表 C::Get/Mget/Mset/Msetnx/Del/MemoryUsage 与 hash/set/list/zset 对象族全接，唯独 basic 字符串与键管理两族缺席），落 `_` 兜底直接回 "-ERR command requires asynchronous completion"。复现：SET k v → DEBUG FLUSHANDEVICT → SETNX k v2 / TTL k / APPEND k x / OBJECT ENCODING k 全部回该错误；C# 同命令经 Tsavorite 同步上下文 CompletePending 阻塞闭环正常应答，客户端永不可见此错。慢路径内核大半已在：mainstore/advanced_ops.rs rmw_main_store（INCR/DECR/APPEND/SETRANGE 共内核）、main_store_ops.rs getdel/setex/append/setrange、increment_by_float，现仅事务域 txn_proc_view 与 AOF 回放 aof_processor 消费，命令慢分派表漏接线。修法：exec_slow_impl 补字符串/键管理两域分派臂接既有内核（缺的 TTL 族/EXISTS/RENAME 按读侧三域探针补异步裁决）。
    rust：wedb/wnode/src/resp/garnet_api/slow.rs:63 exec_slow_impl（分派表与 `_` 兜底）；降级出口例证 wedb/wnode/src/resp/basic_commands/set.rs:373-381 network_setnx、:197-205 network_set、wedb/wnode/src/resp/basic_commands/incr.rs:109/124 network_increment、wedb/wnode/src/resp/key_admin_commands/keys.rs:245 network_expire、:269 network_persist、:309 network_ttl、:141-157 network_getdel、wedb/wnode/src/resp/key_admin_commands/types.rs:205 network_exists、wedb/wnode/src/resp/basic_commands/mod.rs:704 network_object
    C#：garnet/libs/server/Resp/BasicCommands.cs:NetworkSET/NetworkSETNX/NetworkSETEX/NetworkAPPEND/NetworkINCR 族、:1644 NetworkOBJECT（同步执行遇 pending 由 Tsavorite CompletePending 闭环）；garnet/libs/server/Resp/KeyAdminCommands.cs:NetworkEXPIRE/NetworkTTL/NetworkPERSIST/NetworkRENAME/NetworkGETDEL（同闭环形态）
