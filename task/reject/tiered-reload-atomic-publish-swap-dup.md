重复：task/ing/tiered-reflux-atomic-swap.md（关键符号 publish_tree_from_snapshot_locked/PublishMigratedIndex 命中）
优先级：高

分层重灌臂先物理销毁旧树再重建新树，两步之间崩溃键整体蒸发；同仓迁移发布已有 rename 原子换入内核（对标 C# PublishMigratedIndex）却未被重灌路径复用
  具体问题：apply_rmw_post_operate 重灌分支（tiered 键物化写回的常规路径，GEOADD/未支持原生树内臂的操作穿透均达）执行序为 handle_bftree_drain_and_delete(key, true)（object_store_utils.rs:992——meta 物理墓碑落盘 + delete_index 摘注册并投递树文件删除）→ promote_collection_to_bftree（:999 建新树、发流、落新 meta）。两步之间崩溃：旧 meta 已墓碑、旧树投删、新 meta 未落、信封本就不存在（键早已是分层态）→ 重启后键整体消失，孤儿树文件残留且无引用（仅同名重建时被 create_bftree_internal 的旧工件清理回收）；AOF 完整时可经 RangeIndexDrop + RangeIndexStream 序列重放找回，无 AOF/嵌入式/AOF 尾部截断即数据全丢。先拆后建的根因是 create_bftree_internal 对 live_indexes 已有条目回 IndexExists（lifecycle.rs:105-109），迫使重建必须先摘旧树；而同仓 MIGRATE 发布路径 publish_tree_from_snapshot_locked（lifecycle.rs:440 起）已是「临时路径建树 → Unix rename 原子换入（注释自陈换入窗口内无文件缺失间隙）→ 数据+目录 fsync 双屏障 → 锁内排空旧树」的原子换入形态，重灌臂未复用该内核。C# 对象域等价操作（RMW 重写/CopyUpdater）单条日志记录原子完成，无销毁重建窗口。修法：重灌臂改走「新树灌入临时路径 → 快照 → publish_tree_from_snapshot_locked 原子换入 → 新 meta 落盘 → 最后销毁旧树」或等价的先建后拆序，把崩溃窗口从「数据丢失」降为「旧域泄漏」
  rust：wedb/wnode/src/resp/objects/object_store_utils.rs:986-1005 apply_rmw_post_operate 重灌分支（:992 drain、:999 promote）；wedb/wbftree/src/manager/lifecycle.rs:105-109 create_bftree_internal IndexExists 拦截、:440-470 publish_tree_from_snapshot_locked 原子换入先例；wedb/wkv/src/range_index/stub.rs:45-71 drain 内核；可达面 wedb/wnode/src/resp/objects/sorted_set_geo_commands.rs:736 obj_writeback_tiered
  C#：garnet/libs/server/Storage/Functions/ObjectStore/VarLenInputMethods.cs:33-44 GetRMWModifiedFieldInfo（对象重写单条日志原子）；garnet/libs/server/Resp/RangeIndex/RangeIndexManager.Replication.cs:352 PublishMigratedIndex（原子发布内核，rust 侧 lifecycle.rs:440 已有对位件）；规范源 .agents/skills/transpile/SKILL.md:27-30「超大规模或大体积集合自动透明就地升阶」条 + doc/zh/collection.md:66「重启无损恢复」
