重复：task/ing/replica-replay-checkpoint-end-arm.md（关键符号 CheckpointEndCommit/set_in_fuzzy_region/add_new_checkpoint_entry 命中）
优先级：中
副本重放 CheckpointEndCommit 臂缺本地拍 checkpoint 分支，副本 WAL 只增不减且无本地检查点基线
  具体问题：C# 在 AOF 重放遇到检查点结束标记且为主端新版本时（asReplica && header.storeVersion > store.CurrentVersion）同步拍本地检查点：非 sharded 直接 BlockingWait(TakeCheckpointAsync)（AofProcessor.cs:307），sharded 经 ProcessSynchronizedOperation(LeaderBarrierType.CHECKPOINT) 栅栏拍（:315-321）；拍完由 DatabaseManagerBase.cs:528-535 检查点完成回调 AddNewCheckpointEntry → ClusterProvider.cs:170-186 SafeTruncateAOF（副本分支取重放臂先前在 checkpoint start 标记处记下的 ReplicationCheckpointStartOffset 物理截断本地 AOF），同时 store.CurrentVersion 随新 token 推进。rust 对应重放臂 CheckpointEndCommit 只做模糊区收尾（set_in_fuzzy_region(false) + process_fuzzy_region_operations + clear），无任何拍 checkpoint 调用；前置记账面在位（replica_replay_task.rs:177-184 遇 checkpoint start 标记已 set_sublog_checkpoint_start_offset），下游截断面也在位（cluster_provider.rs:1246-1247 副本角色取该位点、:1280 add_new_checkpoint_entry → :1204-1210 副本分支 truncate_until_async），唯独中间触发面整链缺失：全仓 take_checkpoint 生产调用只有 SAVE 面 take_on_demand_checkpoint（cluster_provider.rs:1049），副本只读拒写，副本本地 wal 从建立到断链只增不减（cluster_replication_session.rs 落盘面仅有 fast truncate 跳跃重对齐，无截断），副本重启亦无本地检查点可用只能全量重放本地 wal 再 attach。与 next/glm.net.md 条「副本恢复链 store 版本收敛缺失」边界：那条管 diskless/diskbased 两恢复体一次性 SetVersion 缺失，本条管稳态重放臂的持续拍 checkpoint/截断动作缺失，修完那条本条问题仍在。修法：aof_processor.rs CheckpointEndCommit 臂补 as_replica && header.store_version > target.store_version 时经现有 checkpoint 拍摄面（wnode database_manager_base take 链或 wcpr create）拍一次，走通 add_new_checkpoint_entry 既有回调（多回放拓扑下对齐 C# 经 LeaderBarrierType::CHECKPOINT 栅栏）。
  rust：wedb/wnode/src/aof/aof_processor.rs:533-554 AofProcessor CheckpointEndCommit 臂；记账面 wedb/src/server/replication/replica_replay_task.rs:177-184；截断面（无触发）wedb/src/server/cluster_provider.rs:1240-1253 on_checkpoint_initiated、:1259-1283 add_new_checkpoint_entry、:1196-1212 safe_truncate_aof 副本分支
  C#：libs/server/AOF/AofProcessor.cs:302-323（CheckpointEndCommit 臂内 TakeCheckpointAsync :307、sharded 栅栏 :315-321）；触发链 libs/server/Databases/DatabaseManagerBase.cs:528-535（检查点完成回调 AddNewCheckpointEntry）；libs/cluster/Server/ClusterProvider.cs:170-186（SafeTruncateAOF 副本分支 TruncateUntil）；主端标记写入面对照 libs/cluster/Server/Replication/ReplicationManager.cs:328-343 CheckpointVersionShiftStart/End
