重复：task/ing/zset-o1-bypass-expiry-count.md（关键符号 ZCARD/zset 内存计数/O(1) 旁路过期 命中）
优先级：中

ZCARD 与 zset 计数消费面的 O(1) 承诺 zset 半边断裂：内存操作臂 count() 为 O(带 TTL 成员数) 逐键过期过滤，与同命令已建成的信封 4B 头部 / 分层 MetaValue.size 两态 O(1) 口径分裂；在册 hlen 票现状/方向/验收均只写 hash 半边
  具体问题：SKILL「HLEN / SCARD / ZCARD / LLEN：直读 wcol 内存对象计数 O(1)」与 doc/zh/collection.md:72「全部计数命令（HLEN、SCARD、ZCARD、LLEN 与 RI.COUNT）均严格保证 O(1)」、:76 字段级失效旁路计数抵扣条款。实际 zset 内存计数为 O(K) 过滤形态：SortedSetObject::count() 在 expiration_times 在册时 times.keys().filter(is_expired).count() 逐键过滤，而 ZEXPIRE/ZTTL/ZPERSIST 命令面在位（write.rs:572 对标 C# SortedSetExpire）可令其常态化非空。消费面：ZCARD 内存操作臂 sorted_set_length 直调固有过滤版 count()——全成员 ZEXPIRE 的大 zset 每次 ZCARD O(K)；BZPOPMIN/BZPOPMAX 阻塞前逐键 obj.count()、ZADD/ZMPOP/ZUNIONSTORE/ZRANK 族与集合项源同走过滤版。同仓已兑现对照面：ZCARD 慢路径 obj_length_sync/obj_length_async 信封 4B 头部计数 O(1) 直读（to_blob 头部写 serialize_wire 过滤后计数）、分层态 MetaValue.size + next_expiry 水位快路径——同一命令内存态 O(K)、磁盘态 O(1)，违反「严格 O(1)」且两态口径分裂。另 trait IGarnetObject::count 对 zset 取 sorted_set_dict.len() raw len（should_promote/should_demote 输入，O(1) 不过滤），与具体类型固有过滤版 count() 双 count 同名两语义并存（zset 半边升阶判定无 O(K) 写热路径问题，但同名易混且与 hlen 票方向 2 的 hash 侧论述形态相反）。C# 对位：SortedSetObject.Count() 同为 O(K) 过滤——rust 忠实转写无转写缺口，缺口是 SKILL/doc 自研承诺未兑现（与 hlen 票同论据）。修法：并入 next/hlen-o1-bypass-expiry-count.md 同批同口径（旁路过期计数标量或堆序 purge 后直读 len，禁两套存活计数口径），该票现状/改动方向/验收同步扩 zset 半边；BZPOP/ZADD 等消费面随 count() 单点修复自动受益。边界：不重开 hlen 票 hash 半边（hash_object.rs:563 同型由该票承接）；SCARD/LLEN 无成员级 TTL（C# SetObject 无 expirationTimes，rust set/list count raw len 即正确）不涉本条
  rust：wedb/wcol/src/zset/sorted_set_object.rs:564-571 count() 固有过滤版、:900-906 to_blob 头部计数对照面；ZCARD 臂 wedb/wcol/src/zset/sorted_set_object_impl.rs:313-319 sorted_set_length、:578/:828/:960 ZRANGE byIndex/ZRANK 族；阻塞面 wedb/wnode/src/resp/objects/sorted_set_commands/blocking.rs:64/:68；写命令面 wedb/wnode/src/resp/objects/sorted_set_commands/write.rs:306/:410/:413/:716/:922 与 :572 ZEXPIRE 入口；项源 wedb/wnode/src/resp/objects/collection_item_source.rs:240；两态 O(1) 对照 wedb/wnode/src/resp/objects/object_store_utils.rs:413-476 obj_length_sync/obj_length_async；trait 双 count wedb/wcol/src/types/garnet_object.rs:66/:72/:317-319
  C#：garnet/libs/server/Objects/SortedSet/SortedSetObject.cs:606-617 Count()（O(K) 过滤同型，忠实转写）、:154/:161 expirationTimes/expirationQueue；命令面 garnet/libs/server/Resp/Objects/SortedSetCommands.cs SortedSetExpire；规范源 .agents/skills/transpile/SKILL.md:47-50 O(1) 复杂度计数规约 + doc/zh/collection.md:72/:76 严格 O(1) 承诺；边界：next/hlen-o1-bypass-expiry-count.md（hash 半边在册票，本条为其 zset 同族扩展，不重复立项）
