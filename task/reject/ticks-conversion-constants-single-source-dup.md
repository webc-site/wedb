重复：task/ing/tick-scale-conversion-single-source.md（关键符号 tick 换算常量/10000000 裸乘/wmetric 命中）
优先级：低

tick 换算常量三种形态并存：换号回收延迟裸乘 10000000 绕开单点，wconn/wmetric 各自本地派生同一微秒商
  wbase::convert::TICKS_PER_SECOND（wbase/src/convert.rs:11，10_000_000）是全仓单点，wnode 侧 basic_commands/get.rs:6、ttl.rs:5 等规范消费；但 wkv/src/store/keyspace.rs:107（flush_database 内 expired_at = now + db_gc_reclaim_delay_secs * 10000000）与 :163（flush_ns 同式）生产代码裸乘 10000000 做秒→tick 换算，同文件 :4 已 use wbase::time::now_ticks、wkv Cargo.toml:34 已依赖 wbase，单点零成本可达却绕行——10000000 的单位语义（.NET ticks/秒）全靠读者自行推断；同时 wbase::convert 缺 TICKS_PER_MICROSECOND 成员，导致两 crate 各自补丁：wmetric/src/latency/latency_metrics_entry.rs:44-54 time_stamp 模块自建 TICKS_PER_SECOND 重导出 + TICKS_PER_MICROSECOND/TICKS_PER_SECOND_UNIT 派生组 + seconds() 辅助（注释自称「派生自 wbase::convert::TICKS_PER_SECOND 单处定义」），wconn/src/metrics.rs:30-31 又一份 `const TICKS_PER_SECOND: u64 = convert::TICKS_PER_SECOND as u64` + `TICKS_PER_MICROSECOND = TICKS_PER_SECOND / 1_000_000` 本地 const——同一语义常量（tick→微秒商）在两个 crate 重复定义。C# 对位单点严格：HdrHistogram 库内 OutputScalingFactor 一处定义 TimeStampToMicroseconds，服务端 GarnetLatencyMetrics.cs/RespSlowlogCommands.cs/RespServerSession.cs 共用库常量不自建；秒→tick 一律走 TimeSpan（CollectionItemBroker.cs:44 `TimeSpan.FromSeconds(MIN_SECS...).Ticks`，无裸乘）。修法：wbase::convert 增补 TICKS_PER_MICROSECOND（可并 seconds() 辅助），keyspace.rs 两处、wmetric time_stamp、wconn metrics 收敛为引用，删除本地派生组。
  rust：wedb/wkv/src/store/keyspace.rs:107,:163（裸乘）；wedb/wmetric/src/latency/latency_metrics_entry.rs:44-54（本地派生组）；wedb/wconn/src/metrics.rs:30-31（本地派生组）；单点 wedb/wbase/src/convert.rs:11
  c#：garnet/metrics/HdrHistogram/OutputScalingFactor.cs（TimeStampToMicroseconds 一处定义，GarnetLatencyMetrics.cs/RespSlowlogCommands.cs 共用）；garnet/libs/server/Objects/ItemBroker/CollectionItemBroker.cs:44（FromSeconds(...).Ticks 库换算形态）
