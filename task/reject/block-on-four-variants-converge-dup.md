重复：task/ing/block-on-single-source.md（关键符号 ThreadWaker/vector_store_callbacks/block_on 命中）
优先级：高

同步收割 block_on 工具四份手写、行为三种分叉，wbase 单点在位却无人复用
  同一职责（同步上下文内联驱动 compio future）四份定义：wbase/src/future.rs:30 pub block_on（纯 park 版，ThreadWaker 内嵌 :15-27，生产消费仅 wbase/src/group_commit.rs:211）；wedb/src/server/cluster_session/mod.rs:340 pub(super) block_on（Runtime::try_current 内联收割 + park 回退，消费 mod.rs:326、basic.rs:191、basic.rs:474、replication.rs:630 四处 cluster 命令同步段）；wnode/src/resp/acl_store.rs:37 私有 block_on（同 try_current + park 形态，注释自称「与 vector_store_callbacks.rs 同名工具同构」——实现已不成立）；wnode/src/resp/vector/vector_store_callbacks.rs:47 pub(crate) block_on（Runtime::current() 无回退直接 panic，注释 :40-45 明示 park 分支「已删除——io_uring 上必然永久挂起」）。即对同一「无线程上下文时 park 回退去留」问题存在三种答案：vector 版裁定删除、acl 与 cluster_session 两版保留、wbase 版只有 park；且 acl 版「同构」声明与 vector 版现状互相矛盾——vector 的删除理由恰宣判 acl/cluster_session 保留的 park 回退在涉 IO 用例下永久挂起。消费方横跨 wbase/wedb/wnode 三 crate，单点候选 wbase::future 就在却零复用。修法：wbase::future 增 runtime_aware 变体（try_current 内联收割），三处复抄收敛 import，park 回退去留按 vector 版裁定统一（删除则单测改用 Runtime::new 包裹），ThreadWaker 只留 wbase 一份。
  rust：wedb/wbase/src/future.rs:30；wedb/wedb/src/server/cluster_session/mod.rs:340；wedb/wnode/src/resp/acl_store.rs:37；wedb/wnode/src/resp/vector/vector_store_callbacks.rs:47；第三消费方 wedb/wnode/src/storage/session/txn_proc_view.rs:30（import vector 版；task/done/txn-proc-storage-api-injection.md:22 曾以 acl/vector 两处为「同构先例」新增消费，未指出双定义分叉本身）
  c#：garnet/libs/common/AsyncUtils.cs:22-49 BlockingWait 四重载一处定义全仓共用（cluster_session 版注释自认对标它）；同步收割对位 ClientSession.CompletePending(wait:true) 亦单点
