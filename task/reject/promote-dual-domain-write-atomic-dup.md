重复：task/ing/tiered-drain-envelope-tombstone.md（关键符号 promote_collection_to_bftree/handle_bftree_drain_and_delete/信封删除（ing 票正文自证 stub） 命中）
优先级：高

升阶双域换写非原子（元记录与信封删除两条独立物理写），模块头自称「同帧完成」失真；分层排空链全部不清理信封域，双态残留键删空后以升阶前旧数据复活
  具体问题：promote_collection_to_bftree 收尾是 upsert_raw(meta)（stub.rs:198）与 delete_raw(信封)（stub.rs:200）两次独立 await 物理 write，中间崩溃/掉电或 AOF 部分回放（RangeIndexStream 流块与信封 StoreDelete 墓碑之间截断）即留下「信封旧快照 + Meta 存根 + 树」双态；wedb/wkv/src/session/collection.rs:9-11 模块头注释宣称「落元记录与删信封同帧完成」，与两条 await 写的实现直接矛盾（spec 漂移先例 spec-doc-drift-gcbarrier-ri-promote）。双态期命令路由 Meta 优先（object_store_utils.rs:258/:331/:391「优先探测 Meta 分层态」）读写走树无误，但分层态排空/删空链全部只清 Meta+树+TTL 不清信封：drain_or_save 删空臂（tiered_collection_ops.rs:369-390）、apply_rmw_post_operate 删空臂（object_store_utils.rs:974）、handle_bftree_drain_and_delete（stub.rs:45-71）、RI 排空与 STORE 族清退同走该内核——双态键删空后 Meta 消失，命令回落信封域（contains_key/read_tag_with 双域探测），已删空的集合以升阶时刻的完整旧数据复活（EXISTS/HGETALL 全量可见），正是 SKILL 删空自愈条要杜绝的幽灵形态。对照 DEL 干净面：wkv session/collection.rs:26-65 delete() 复合臂在 drain 之后仍对 String/Envelope 双域墓碑，证明双域清理是既知必要，唯独分层排空链漏掉信封。另外 stub.rs:200 `let _ = self.delete_raw(&env_k).await` 静默吞错：信封删除 IO 失败（无需崩溃）同样留下永久双态而函数照常返回 Ok。修法：排空/删空内核（handle_bftree_drain_and_delete 或其上层 drain_or_save）连带对信封域幂等墓碑（与 DEL 复合臂同口径一处收口），promote 信封删除失败上抛或留痕；collection.rs 模块头「同帧完成」措辞同步改写为真实写序
  rust：wedb/wkv/src/range_index/stub.rs:107-214 promote_collection_to_bftree（:198 upsert_raw meta、:200 吞错删信封）、:45-71 handle_bftree_drain_and_delete；wedb/wkv/src/session/collection.rs:9-11 注释、:26-65 delete 双域对照面；wedb/wnode/src/resp/objects/tiered_collection_ops.rs:369-390 drain_or_save 删空臂；wedb/wnode/src/resp/objects/object_store_utils.rs:974 apply_rmw_post_operate 删空臂
  C#：无分层对位（分层为 SKILL.md:27-30 自研扩展）；同域「单条原子换域」对位 garnet/libs/server/Storage/Functions/ObjectStore/VarLenInputMethods.cs:33-44 GetRMWModifiedFieldInfo（对象记录重写一条日志追加完成，无两步窗口）；规范源 .agents/skills/transpile/SKILL.md「严格删空生命周期与原子墓碑……杜绝幽灵空元记录与孤儿 TTL」条 + doc/zh/collection.md:66（5.3 节「Checkpoint 快照与 AOF 重放器天然统一两态协议，重启无损恢复」）
