重复：task/ing/rmw-atomic-read-modify-write-window.md（关键符号 network_increment/try_rmw_sync/read_user_sync 命中）
优先级：高

主存与信封域全部读改写命令的「读-算-写」三段无同键互斥，多核并发同键丢失更新；去条带锁的自定义优化把 C# 引擎锁内原子 RMW 的语义一并丢弃（与 glm.db.md 第 3 条 String 域明细同根，本条为其超集，修法应一处收口、拆单勿重复立项）
  具体问题：C# 的 INCR/APPEND 等读改写命令只把增量封入 RMW input 交引擎，旧值读取、计算、写回全部在 Tsavorite bucket 锁 + epoch 内完成（RMWMethods.cs 的 InPlaceUpdater/CopyUpdater），并发同键天然串行；对象域同构（ObjectStore/RMWMethods.cs 记录锁内对 IGarnetObject 执行 op）。rust 把三段拆到命令层：先 read_user_sync 读旧值，命令层算新值，再 try_rmw_sync 盲写——try_rmw_sync 底层 try_upsert_raw_sync_unprotected 的 CAS 循环只做单次写挂链定序，无前值校验、无读改写窗口锁。多 worker 线程（SO_REUSEPORT 每核一 worker）并发同键即丢失更新：两核并发 INCR 同键各读 5 各写 6（丢一次自增）；并发 APPEND 丢一次追加；并发 HSET 同键不同字段经 run_sync_rmw（obj_load_typed_sync 反序列化 → run_op 修改 → obj_save_or_gc_raw 整值写回）后写者整体覆盖前写者（丢字段）——信封是整值写回，前写者的字段级修改直接蒸发。wkv 侧通用入口 rmw_raw 同样是 read_raw_with → updater → upsert_raw 两个 await 分离，注释自称「严格对照 C# Tsavorite InternalRMW 状态转移机」但丢失了其锁语义。仓内并非无先例：EXPIRE/PERSIST 已持 index.acquire_keys_lock_exclusive 本键独占桶锁串行读改写窗口（ttl.rs 注释明言无锁时 ttl_of → del_ttl 间隙并发可产生用户可见异常），wkv 也有 try_modify_raw_in_place_unprotected 页写锁内原位读改写（但 INCR/APPEND/run_sync_rmw 均未走它，且它只覆盖内存可变区同尺寸情形，无 CopyUpdater 对等的锁内重试）；session/mod.rs:779 自述「rust 主存储为 compio 每核单线程 + epoch 无锁写，无条带锁可分组」——该声明只对 MSET 批量折叠的差异正当，被外推到了 RMW 原子性。与 next/tiered-write-arm-concurrency.md 同根不同面：该票只管分层（tiered）树内写臂的同键互斥，本条管主存 string RMW 族与内存信封 RMW 通用内核两个域，修法应一处收口（读改写窗口统一持本键桶锁，或 upsert 增加 expected-address CAS 失败重算协议）。无并发 INCR/HSET 回归测试兜底（wnode/tests 无并发同键读改写用例）
  rust：wedb/wnode/src/resp/basic_commands/incr.rs:96-122 network_increment（read_user_sync → checked_add → try_rmw_sync 盲写）、:155-192 network_increment_by_float 同型；wedb/wnode/src/resp/basic_commands/set.rs:607-645 network_append 同型；波及面同型：wnode/src/resp/bitmap/bitmap_commands.rs（SETBIT/SETRANGE/BITFIELD 写臂）、wnode/src/resp/hyperloglog/hyper_log_log_commands.rs（PFADD/PFMERGE）；信封通用内核 wnode/src/resp/objects/object_store_utils.rs:1161-1237 run_sync_rmw（HSET/ZADD/SADD/LPUSH 等全部集合 RMW）；wkv 侧 wedb/wkv/src/session/raw/modify.rs:93-118 rmw_raw（两段 await 分离）、:18-82 try_modify_raw_in_place_unprotected（原位原子但未被上述路径使用）、wkv/src/session/raw/write/rmw.rs:34-104 try_rmw_sync/upsert_rmw（无前值校验）；桶锁先例 wedb/wkv/src/ttl.rs:450/:497-499 acquire_keys_lock_exclusive
  C#：garnet/libs/server/Resp/BasicCommands.cs:852 NetworkIncrement（new StringInput(cmd, 0, incrByValue) 交 storageApi.Increment，命令层不读旧值）、:902 NetworkIncrementByFloat、:959 NetworkAppend；引擎锁内读改写 garnet/libs/server/Storage/Functions/MainStore/RMWMethods.cs:230-273（INCR/DECR 族 InitialUpdater 组装增量）与 :420/:437 InPlaceUpdaterWorker（锁内读 logRecord.ValueSpan 算后写回，TryInPlaceUpdateNumber/TryCopyUpdateNumber）；对象域 garnet/libs/server/Storage/Functions/ObjectStore/RMWMethods.cs（记录锁内 RMW op）；规范源 .agents/skills/transpile/SKILL.md:10「尽量 1:1 对标 C# 的代码实现」+ SKILL.md:21-22「迭代序稳定由连接任务线程钉定承接」（连接钉线程只保证单会话内串行，跨连接并发同键无承接面）
