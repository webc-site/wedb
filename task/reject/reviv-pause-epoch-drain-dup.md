重复：task/ing/reviv-pause-epoch-drain.md（关键符号 RevivPauseGuard/pause-epoch-drain 标题一字不差 命中）
优先级：中
迁移复活暂停只落 pause 半协议、缺纪元排空等待：C# PauseRevivification 带 BumpCurrentEpoch+事件等待强同步，rust RevivPauseGuard 无排空即开扫，pause 瞬间在途复活写者可在键枚举窗口内改写中部槽位致漏迁键滞留源端
  具体问题：C# SLOTS/SLOTSRANGE 迁移后台任务全程暂停复活，且暂停是两半协议：RevivificationManager.PauseRevivification()（挂起计数递减，挡新 take）之后必须 epoch.Resume → BumpCurrentEpoch(() => pauseRevivEvent.Set()) → pauseRevivEvent.Wait(timeout, token)，即「等所有持纪元的在途复活写者排空、确认暂停信号被全员观察到」才返回开扫（Tsavorite.cs:121-146，注释 "Wait for everyone to observe the reviv suspend signal"）；迁移结束 finally ResumeRevivification。暂停动机：复活会把中部空闲槽位改写为新键并原子挂链，键枚举游标已过该桶位/槽位时新键漏收，漏收键既不传输也不删除，滞留源端（槽已迁出后不可达成孤儿键）。rust 只落了前一半：RevivPauseGuard::new 仅调 reviv_pool.pause()（wreviv 挂起计数递减，挡新 take），execute_slots_migration 构造 guard 后直接建会话、连迁移客户端、进逐槽取键循环，全程无任何 epoch bump/排空等待；pause 生效前已 take 出槽位的在途复活写者（revivify_record_at 裸写 + 索引挂链）不受 pause 影响，可在扫描推进中完成改写，上述漏迁窗口在 rust 未闭合。wreviv 侧注释自述该契约（「无全局栅栏语义，强同步暂停需配合上层 epoch 排空……由调用方按需组合」，对标 C# BumpCurrentEpoch + 事件等待协议），但全仓唯一调用方没有组合，协议缺半。修法：RevivPauseGuard::new 在 pause 之后对写者纪元做 bump+排空等待再返回（写路径持纪元守卫已有基础：会话操作一律经 enter_gated → store.barrier_enter；同文件 slots.rs:176-179 在 TRANSMITTING/DELETING 前已用 bump_and_wait_for_epoch_transition_async 做过同型「纪元静止等待」），Drop 时 resume 不变。边界：与 task/ing/migrate-slots-copy-delete-gate.md（删除环 copy 门）、task/ing/migration-frame-import-core.md（导入侧帧管线）不同面；与 next/tiered-write-arm-concurrency.md 不同域（wcol/wbftree 分层写臂）；wtxn 锁表条与复活无关。属 enable_revivification 开启时才暴露的窄窗口（理论竞态），但 C# 该排空是显式设计而非可有可无。
  rust：wedb/wedb/src/server/migration/migrate_driver/slots.rs:84-107 RevivPauseGuard（:93 pause、:100 resume，无排空）、:109-131 execute_slots_migration（:114 构造 guard 后直接扫描编排）；复活写入面 wedb/wkv/src/session/raw/mod.rs:116-155（take + revivify_record_at）；契约自述 wedb/wreviv/src/pool.rs:180-199 pause/is_enabled 文档；既有同型排空原语先例 wedb/wedb/src/server/cluster_provider.rs:732-741 bump_and_wait_for_epoch_transition_async（slots.rs:176-179 消费）；写路径纪元基础 wedb/wkv/src/session/mod.rs:245-251 enter_gated
  C#：libs/storage/Tsavorite/cs/src/core/Index/Tsavorite/Tsavorite.cs:121-146 PauseRevivification（pauseRevivEvent.Reset + BumpCurrentEpoch(() => pauseRevivEvent.Set()) + pauseRevivEvent.Wait(timeout, token)）与 :148-149 ResumeRevivification；调用点 libs/cluster/Server/Migration/MigrationDriver.cs:139（BeginAsyncMigrationTaskAsync 开头 PauseRevivification(_timeout, _cts.Token)）与 :225（finally ResumeRevivification）；实现 libs/storage/Tsavorite/cs/src/core/Index/Tsavorite/Implementation/Revivification/RevivificationManager.cs:26-28
