重复：task/ing/lua-call-pending-suspend-handoff.md（关键符号 redis.call 挂起/Suspended/快路径 命中）
优先级：高

lua redis.call 重入面（含 SET/GET 快路径）对慢路径/阻塞两类挂起零承接，冷键命令回内部错误且挂起应答错插网络流
    EVAL 同步执行（run_lua_command）内 redis.call fallback 经 RespScriptingApi::dispatch_resp 重入 try_consume_messages，其 loop 只处理 take_output_watermark_yield，无 take_slow_wait/take_blocked_wait 承接。命令命中磁盘候选/环形页翻转降级（exec 挂 session.pending_slow）或 BLPOP 族挂 pending_block 时：1) process_messages 循环 break（:1260）后 dispatch_resp 收集的 session.output 无该命令应答，process_resp_response_view 对空输入走 "Unexpected response, this should never happen" 分支回 UNEXPECTED_ERROR，redis.call 报内部错误而非命令应答；2) 挂起残留会话上，脚本内后续每条 redis.call 因 process_messages 入口门（:1132 pending 挂起即 return 0）同样拿空应答，且 EVAL 结束后挂起 future 被网络泵 drive.rs:176 take_slow_wait resolve，其应答字节直接写网络输出——错插一帧不属于任何客户端命令位置的应答，客户端 pipelining 应答错位。SET/GET 快路径（RespScriptingApi::get/set 内部同走 dispatch_resp 拼 RESP 请求）同样命中：GET 冷键即降级（exec_slow_impl C::Get 臂存在，证明该命令确实降级）。复现：SET k v → DEBUG FLUSHANDEVICT → EVAL "return redis.call('GET', KEYS[1])" 1 k。C# 同场景：ProcessCommandFromScripting 尾部同步 `respServerSession.TryConsumeMessages(request)` 后 GetResponse，C# 命令同步执行遇磁盘 pending 由 Tsavorite 同步 API 内部 CompletePending 闭环（无 SlowWait 概念），lua 内命令恒拿完整应答、协议流不错位。修法：dispatch_resp 循环补挂起承接（同步 resolve 或 lua 面专用同步驱动），或在 lua 窗口对降级信号走同步阻塞读。
    rust：wedb/wnode/src/resp/resp_server_session.rs:2709 RespScriptingApi::dispatch_resp（loop 无挂起臂）；挂起点 wedb/wnode/src/resp/garnet_api/mod.rs:558 exec 挂 pending_slow、wedb/wnode/src/resp/resp_server_session.rs:1132 入口门、:1260 循环挂起 break；网络泵独占消费点 wedb/wnode/src/net/handler/drive.rs:176
    C#：garnet/libs/server/Lua/LuaRunner.Functions.cs:3137 ProcessCommandFromScripting（:3311 TryConsumeMessages 同步闭环 + :3313 GetResponse）；C# 同步 pending 闭环由 Tsavorite 同步 API 承接，lua 重入路径无异步残留形态
