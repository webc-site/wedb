重复：task/ing/range-index-replication-shutdown-dispose.md（关键符号 RangeIndexManagerReplication::dispose/StoreWrapper 命中）
优先级：中

RangeIndexManagerReplication::dispose 全仓零调用，停机链缺 C# StoreWrapper.Dispose 的范围索引收口步骤：写了 dispose 无人调（broker 双票同族第三件），Drop 只承接半边（仅 reassembly 清理），全部在线树释放完全依赖 WedbStore 析构时 Arc 归零兜底
  具体问题：rust 的 dispose()（体内两步：dispose_incomplete_stream_reassembly + self.engine.dispose()）生产与测试两侧全仓零调用——两处构造点（service.rs 的 AofSinkContext 与回放会话装配）均从不调用，测试面也只直接调 dispose_incomplete_stream_reassembly；同文件 Drop 实现只做 reassembly 清理不做 engine 释放，而 engine（Arc<wbftree::RangeIndexManager>，与 store.range_index 共享同一 Arc）的全部在线树释放唯一显式挂点在 WedbStore::drop 内，即停机收口从「显式步骤」退化为「依赖所有 Arc 引用（service/ctx/会话/detach 任务）恰好归零」的隐式析构：嵌入式或复用进程场景下 stop() 返回后任一滞留引用即令全部树句柄与 native 页缓存驻留不释，且 dispose() 死面自居「1:1 对标 RangeIndexManager.cs:Dispose」却无任何调用方，属 SKILL「严禁占位函数或虚设实现」射程内的虚设面。C# 对位链完整：StoreWrapper.Dispose 第十步 rangeIndexManager?.Dispose() 为真实停机调用，RangeIndexManager.Dispose 逐树 Dispose 并清 liveIndexes，时序在 clusterProvider.Dispose 与 databaseManager.Dispose 之间。修法二选一不留静默分叉：停机链（server.rs 统一尾部或 GarnetServer::stop）显式补 ri 收口调用（时序对标 C#，置 aof dispose_async 之前）；或复核裁定 Drop 兜底为 rust 刻意范式，则删除 dispose() 死面（保留 Drop 的 reassembly 臂）并在 wbftree manager 层文档声明「树释放由 Arc 归零析构承接」的取舍。边界：与 task/ing/subscribe-broker-shutdown-dispose.md（pubsub broker）、task/ing/itembroker-shutdown-dispose.md（集合项经纪）为同族不同件，本条是范围索引回收面；zero-consumer 四批与 wbftree-service-noop-dead-surface.md 射程清单均不含本符号（前者管 pub 死面普查、后者管 BfTreeService::noop FFI 基准函数）
  rust：wedb/wnode/src/rangeindex/range_index_manager_replication.rs:595-598 dispose（:597 engine.dispose）、:612-617 Drop 只清 reassembly；构造点 wedb/wnode/src/service.rs:377/:463 两处均不调；engine 共享 wedb/wkv/src/store/mod.rs:95 range_index、:605 WedbStore::drop 内 range_index.dispose()（唯一显式释放挂点）；wbftree 层 Drop 兜底 wedb/wbftree/src/manager/mod.rs:542-545；停机链 wedb/wnode/src/server.rs:701-716 stop 与 :301-308 统一尾部均无 ri 收口步
  C#：garnet/libs/server/StoreWrapper.cs:909-924 Dispose（:921 rangeIndexManager?.Dispose()）；garnet/libs/server/Resp/RangeIndex/RangeIndexManager.cs:473-491 Dispose（reassembly 清理 + 逐树 Tree?.Dispose() + liveIndexes.Clear()）
