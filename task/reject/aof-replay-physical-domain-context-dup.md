重复：task/ing/aof-replay-virtual-domain-context.md（关键符号 set_context/KeyContextGuard/flush_database 命中）
优先级：高

AOF 回放域模型与从库继承承诺断裂：KeyContextGuard 与 FLUSH 族回放臂把条目物理虚拟域 (vns,vdb) 当逻辑域灌 set_context/flush_database，从库本地二次映射再物化；空库回放「自洽」纯因回放与探针两错相消，checkpoint 基线 + AOF 尾部标准形态下增量落伪映射域不可见、vns 撞在册 logic_ns 时跨租户串数据
  具体问题：入账侧 keyed 条目键一律为引擎物理键 [vns][vdb][tag][payload]（on_aof_store_event 的 Write/TtlWrite/EtagWrite 臂，enqueue_raw 原样透传，physical_key 注释自述「ns/db 取事件携带的会话真值」），FlushDb/FlushNs 条目 16B 域载荷 = (vns, 换号前旧 vdb)/(旧 vns, 0)。回放端 KeyContextGuard::enter 解出 (vns, vdb) 后调 batch.set_context——这是逻辑域入口：is_virtual=false、get_or_create_ns/get_or_create_db 为纯内存盲分配原语（绝不点查磁盘），随即为「以物理号值为逻辑键」的伪租户/伪库分配全新虚拟号并 try_persist_dbmeta_sync 落盘伪 DbMeta 映射。正确入口 set_virtual_context(vns, vdb)（is_virtual 直设物理域、零解析零分配）在同仓 keyspace.rs flush_expired_keys、gc.rs 死域清扫已是物理域处理惯例。FLUSH 族回放臂同病：回放闭包把载荷 (vns, old_vdb) 直灌 flush_database(ns, db_id)（逻辑域换号入口：resolve_context + vdb.flush_db(logic_ns, logic_db)）。后果链四层：(1) 空库全量回放「看起来正确」纯因回放与验证探针两错相消——aof_flush_replay.rs:174-186 探针同用 set_context(3,7)/(3,8) 物理域值，测试注释「从库映射跟随主库换号」自证实现心智模型是「从库逻辑域==主库物理域」，与 doc/zh/db.md「从库完全继承主库的映射体系，不进行本地二次映射」直接矛盾，实现正是在做本地二次映射；(2) checkpoint 基线 + AOF 尾部（副本 attach 标准形态、本机崩溃恢复同形）下恢复链已装载真实映射（ns 标量全量 + 根域路由，物理镜像数据在 (vns, vdb) 域），此后尾部 keyed 条目 set_context(vns, vdb)：vns 恰为在册 logic_ns 时撞号解析进该租户既有映射（vns 与 logic_ns 同一 u64 空间，跨租户串数据），否则盲分配伪 vns''/vdb'' 把增量写到伪映射域——failover 或副本读经真实映射 (logic_ns, logic_db) 解析均不可见，回放端落盘的伪 DbMap 还覆盖污染根域 DbMeta；(3) 主库换号后条目域 (vns, new_vdb) 在从库被物化为 logic_db=new_vdb 的幽灵库，FLUSHDB 后新数据漂移到错误库号；(4) 槽位分叉：在线路径 slot_of(namespace, active_db)（逻辑域）vs 回放路径 slot_of(batch.namespace(), batch.active_db()) = slot_of(vns, vdb)——RI/向量登记表槽位键主从两端算出不同值，SKILL「集群以 namespace -> db 为唯一分片」的 (ns,db) 确定性槽位映射在回放面断裂。修法方向：KeyContextGuard 改 set_virtual_context(vns, vdb) 物理域直设；FLUSH 族回放臂改物理域退役语义（从库继承映射体系下 FlushDb(vns, old_vdb) = vns 租户路由表中指向 old_vdb 的库格换号 + gc_dead 记账），不把载荷当逻辑域灌 flush_database；aof_flush_replay.rs 探针改经真实映射装载后按逻辑域验证。边界：task/done/aof-replay-flush-barrier-single-predicate 管多回放任务栅栏竞态（不同面）；task/done/migration-frame-import-core 管 MIGRATE 帧扫描域（不同面）；task/reject/swapdb-replica-replay-catchup 拒的是「造 SWAPDB 传播管线」并明示「物理 (vns,vdb) vs 逻辑 (ns,db) 域值口径」为未解决前置问题，本条即该口径问题在既有 keyed/FLUSH 回放链的落地核实；next/vector-registry-nsdb-isolation 管 vector 登记键 (0,0) 硬编码（不同根，本条对 vector 条目同样生效但根因在 KeyContextGuard）
  rust：wedb/wnode/src/aof/aof_processor.rs:1194-1218 KeyContextGuard::enter（:1212 batch.set_context 逻辑域入口）、:814-830 replay_op 调用点、:578-609 FlushDb/FlushNs 回放臂（:592/:605 载荷物理域直灌）、:931 slot_of(batch 域)、wnode/src/aof/aof_processor_chunk_replay.rs:100 chunked 同型；逻辑域入口与盲分配 wedb/wkv/src/session/mod.rs:312-364 set_context（get_or_create + :349-363 伪映射落盘）、:366-370 set_virtual_context 正确入口、wkv/src/vdb.rs get_or_create_ns/get_or_create_db（纯内存分配不点查磁盘）；物理域先例 wkv/src/store/keyspace.rs:56、wkv/src/gc.rs:473/683；入账键=物理域 wnode/src/service.rs:97-125 enqueue_raw、:170-172 Write 臂过滤、:121-133 physical_key 注释、FLUSH 载荷域 wnode/src/aof/garnet_log/single_log_branch.rs:444-445；换号内核逻辑域参数 wkv/src/store/keyspace.rs:96-141 flush_database、wkv/src/vdb.rs:961-988 flush_db；固化错误模型的测试 wnode/tests/aof_flush_replay.rs:147-195
  C#：garnet/libs/server/AOF/AofProcessor.cs:161-182 SwitchActiveDatabaseContext（多库 AOF 按子日志切库，域值用 db.Id 逻辑库号）、:483-545 ReplayOp keyed 分支（条目键无域前缀直接 Upsert——C# 每库独立 store 键无前缀故无域换算面；rust 单日志 + 物理前缀承载域是自定义优化，回放端必须虚拟域直设）；规范源 .agents/skills/transpile/SKILL.md:32-36（前缀刚性隔离/虚拟 ID 映射/从库回放换号条目）+ doc/zh/db.md:201-207（主从物理镜像「从库完全继承主库的映射体系，不进行本地二次映射」）
