重复：task/ing/garnet-scan-cs-corpus-parse-gate.md（关键符号 check.js/tree-sitter/hasError 194 文件 命中）
优先级：高

check.js 的 C# 函数提取器 tree-sitter 解析失败致 miss 门禁系统性漏报：194/1425 文件 hasError，核心命令文件提取率约一成，「check.js 无缺失输出 = 转写完备」的流程判定失真
  js/check/garnetScan.js 经 treeSitter.js 加载 @2h2d/tree-sitter-wasms 的 c-sharp 语法解析 C#，该语法不支持指针后缀递增赋值等 unsafe 写法（首个炸点形态 `*tmp++ = (byte)'$';`，位于 libs/server/Storage/Functions/MainStore/PrivateMethods.cs:59），此后整段解析退化为碎片（root 直接子节点散出 28 个 ERROR），descendantsOfType("method_declaration") 只能捡到 ERROR 树里偶然成形的少数节点。实测（parser.parse 后 hasError + 提取数对比）：1425 个 .cs 中 194 个 hasError，含 BasicCommands.cs、RespServerSession.cs、AofProcessor.cs、ClusterManager.cs 等核心文件；抽取率 PrivateMethods.cs 仅 1 个（CopyTo，实际约 30+ 方法，:396 TryInPlaceUpdateNumber 实存未提取）、SessionParseState.cs 仅 2 个（:183 EnsureCapacity 实存未提取）、BasicCommands.cs 46 对粗估 432、RespServerSession.cs 15 对粗估 184、AofProcessor.cs 9 对粗估 114。后果链：check.js 的 miss 判定循环只遍历 garnetScan 提取到的函数名（check.js:419-433 all_file_set/fn_li），提取不到即永远不进 miss 也不进 documented 比对——AofAddress.cs 实测 34+ 方法仅 MinExchange/AnyGreater/IsOutOfRange 三个进 check/miss/libs/server/AOF/AofAddress.yml，其余 ~30 个（ToByteArray/FromByteArray/Serialize/Deserialize/MonotonicUpdate/MaxExchange/Diff/AggregateDiff 等）从未被完备性检查触达；transpile SKILL.md「直到 check.js 没有缺失的输出」的验收口径对这些文件形同虚设，js/check/README.md 亦未声明该解析限制。连带：ignore 台账中登记在解析失败文件上的函数条目（如 server.yml:270 TryInPlaceUpdateNumber）当前是「永不参与判定的暗条目」，解析修复后会突然全量激活，届时需整批复核。修法：treeSitter.js 换支持该语法的 c-sharp 语法版本（或对 hasError 文件改用声明正则兜底提取），并在 check() 对 hasError 文件大声报语料失效（对齐其 parse_fail_li 既有纪律）。
  rust（工具链）：js/check/garnetScan.js:25 csExtract（descendantsOfType 仅 method_declaration/local_function_statement）；js/check/treeSitter.js:11 csParser（@2h2d/tree-sitter-wasms c-sharp.wasm）；漏判循环 js/check.js:419-433；受害者样本 check/miss/libs/server/AOF/AofAddress.yml（34 方法仅 3 入册）；js/check/README.md（未声明限制）
  c#：garnet/libs/server/Storage/Functions/MainStore/PrivateMethods.cs:59（`*tmp++ = (byte)'$';` 首个解析炸点，:396 TryInPlaceUpdateNumber 为实存未提取样本）；garnet/libs/server/Resp/Parser/SessionParseState.cs:183 EnsureCapacity；garnet/libs/server/AOF/AofAddress.cs:66-384（34+ 方法仅 3 个被提取）
