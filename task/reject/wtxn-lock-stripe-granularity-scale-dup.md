重复：task/ing/wtxn-lock-stripe-count-parity.md（关键符号 TxnLockTable/STRIPE_COUNT/stripe_index_for_hash 命中）
优先级：中
wtxn 事务键锁表条带数恒 1024 且注释虚构 C# 出处：C# 锁粒度默认 2^21 桶、锁位嵌桶零独立锁表内存、随哈希表扩容自动细化，rust 恒定无扩容挂钩且「内存适配」立论失真
  具体问题：TxnLockTable 以 STRIPE_COUNT = 1<<10 固定条带承接 C# 事务锁，注释自称「1024 个闩字槽位（C# Tsavorite 锁表默认 64K 桶的内存适配，条带内冲突由桶级并发语义承接）」，三处失真：其一 C# 锁表没有独立内存可「适配」——锁位嵌在哈希桶本体（HashBucket 复用 first overflow bucket entry word 高位作 latch，"We use the first overflow bucket for latching"），Tsavorite 只是持 store 引用按桶定位；其二「默认 64K 桶」数字错：Garnet 默认 IndexMemorySize = "128m"，IndexSizeCachelines 按 64B/桶换算即 2^21 = 2,097,152 桶，rust 写小 2048 倍；其三 C# 锁粒度随表态动态取（NumBuckets = store.state[resizeInfo.version].size_mask + 1，事务锁 DoTransactionalLock 每键现取 GetBucketIndex(keyHash)），哈希表 split 扩容后锁粒度自动细化，rust 条带数与 windex 表规模完全解耦、无任何扩容跟随，键基数超过 1024 后跨键假冲突率被钳在 1/1024 且永不收敛。影响面：事务验证/提交阶段同条带不同键被迫串行重试（TryLock 失败），高并发多键事务（MULTI/EXEC）吞吐随冲突条带退化；排序面同病——TxnKeyEntryComparison 以 stripe_index_for_hash 为排序键（对标 C# SortKeyHashes 按桶下标排序），排序粒度一并锁死在 1024。修法：条带数升到与 windex 表规模联动（随 resize 翻倍细化，或直接复用桶下标位面），并订正注释三处虚构（删「内存适配」立论与「64K 桶」数字）。边界：与 task/ing/windex-batch-lock-std-dedup-single-source.md 不同面（那张管批量桶锁的排序去重内核，本条管锁粒度规模与出处注释）；正确性无虞（同键恒同条带），属粒度/结构失真 + 注释虚构出处（同 whlog-safe-tail-identity-alias 型）。
  rust：wedb/wtxn/src/txn_lock_table.rs:21-23 STRIPE_COUNT = 1<<10 与注释、:65-69 stripe_index_for_hash（>>20 取高段）；消费面 wedb/wtxn/src/transaction_manager.rs:380-389（lock_table 字段 :383、TxnKeyEntries::new(16, lock_table) :389）、wedb/wtxn/src/txn_key_entry_comparison.rs:33-34（条带下标排序）；装配面 wedb/wnode/src/service.rs:390、:977 TxnLockTable::new()（无尺寸参数，全仓唯一构造路径）
  C#：libs/storage/Tsavorite/cs/src/core/Index/Tsavorite/HashBucket.cs:14-15（锁位嵌桶注释）与 :40-61/:80-116（TryAcquireSharedLatch/TryAcquireExclusiveLatch 直接 CAS 桶 entry word，无独立锁表内存）；libs/storage/Tsavorite/cs/src/core/Index/Tsavorite/Implementation/Locking/OverflowBucketLockTable.cs:14-16（NumBuckets => state[resizeInfo.version].size_mask + 1，锁粒度随 resize 细化）、:29-34（GetBucketIndex/GetBucket 现取当前版本 size_mask）；libs/storage/Tsavorite/cs/src/core/Index/Tsavorite/Tsavorite.cs:227-228（LockTable = new OverflowBucketLockTable(this)，锁表随 store 构造）；事务锁消费 libs/storage/Tsavorite/cs/src/core/ClientSession/TransactionalContext.cs:63-102 DoTransactionalLock 与 :104-155 DoTransactionalTryLock（:79/:125 每键经 store.LockTable.GetBucketIndex 定位锁桶）；默认规模链 libs/server/Servers/ServerOptions.cs:67（IndexMemorySize = "128m"）与 :204-211（IndexSizeCachelines ÷64 换算桶数）、libs/server/Servers/GarnetServerOptions.cs:750-755（IndexSize = indexCacheLines * 64L 注入 KVSettings）
