重复：task/ing/flush-safe-read-only-bound.md（关键符号 SafeReadOnlyAddress/flushed_until/PendingFlushList 命中）
优先级：高
KV 主日志常规刷盘驱动（前台驱逐与组提交 FlushStep）上界越过 SafeReadOnlyAddress，「先分配后编码」窗口内的零/半截记录可被持久化并推进 flushed_until，页驱逐后该记录在设备上永久缺失
  具体问题：whlog 追加协议是「CAS 推进 tail 发布物理空间 → 同线程裸写 encode_at」（append.rs 页内快路径 CAS :104-108 成功后才 :112-113 encode_at，换页臂同款 :160-183），encode_at 不持页锁（mod.rs encode_at :490-505 直接 raw_page_ptr_mut）；C# 消除该窗口的机制是刷盘严格以 epoch 排空后的 SafeReadOnlyAddress 为界：ShiftReadOnlyAddress 用 BumpCurrentEpoch 包裹 OnPagesMarkedReadOnly，后者推进 SafeReadOnlyAddress 后才 AsyncFlushPagesForReadOnly（注释 "Seal: make sure there are no longer any threads writing to the page / Flush"，AsyncFlushPagesForReadOnly 文档 "Called when all threads have agreed that a page range is sealed"）——写者在 session epoch 保护内完成编码，排空即无在途写入。rust 的 whlog flush_pages_range 崩溃一致性契约也自述了同一要求（「调用方须保证待刷盘区间内不存在在途未完成的记录编码（如先推进 ReadOnlyAddress 并等待纪元排空再刷盘，对标 C# 仅刷 SafeReadOnly 以下页面的 OnPagesMarkedReadOnly 驱动策略）」），且 wcpr 检查点路径确实照做（create.rs 先 shift_read_only_address(tail) 再 wait safe_ro >= tail 才 flush_all）；但两个常规刷盘驱动未做：(1) 前台驱逐 evict_pages_for（append 遇 PageNotReady 的重试路径，环形窗口回绕的高频驱动）在 flush_pages_range(target_evict_page 可达 curr_tail_page-1 的完整页) 前无任何 safe_ro 门槛，且 :268 的 shift_read_only_address 在刷完之后才调用（先刷后封，次序与 C# 相反）——「完整页」只保证 tail 越过页界（分配完成），不保证迟到 encode_at 已完成（另一写者线程 CAS 已成功、memcpy 未落）；(2) 组提交 FlushStep::step 直接刷 [flushed_until, target=tail] 含活跃页，自身无门槛（其调用方 wcpr 自带封印故安全，另一调用方 flush_and_evict_all 裸奔）；(3) 同病第三处：shift_begin_address 补刷 [flushed_until, new_begin) 在 shift_read_only_address(new_begin) 注册延迟动作后立即开刷，不等 safe_ro 达标。竞态后果：fill 闭包页读锁与 encode_at 裸写不互斥，拷出全零或半截记录落盘并使 flushed_until 越过该区间；此后页不重刷（flushed_until 只前进）、驱逐时 ensure_page_ready 因 flushed_until 已达标而直接清槽，设备上该记录永久缺失，而写者编码完成后索引照常发布该地址——页驱逐后读走磁盘得零/坏头（PadRecord/RecordCorrupted），重启恢复按 flushed_until 前缀扫描亦跳过零头，记录静默丢失。修法：evict_pages_for/FlushStep/shift_begin_address 补刷前对齐 wcpr create.rs:214-223 既有模式（先 shift_read_only_address(上界) 再等 safe_read_only_address >= 上界后刷），或把常规刷盘上界钳制到当前 safe_read_only_address。边界：与 task/ing/compact-safe-read-only-address-bound.md（紧缩搬移上界用 safe_ro）不同面——那条管 compactor 读旧值搬尾与原位写竞态，本条管刷盘持久化边界与迟到编码竞态，修完那条本条仍在；与 task/done/wkv-flush-step-call-single-source.md（FlushStep 调用收口）无关。
  rust：wedb/wkv/src/session/raw/mod.rs:232-277 evict_pages_for（:243-245 target 计算、:256-261 刷前无门槛、:268-269 刷后才 shift_ro）；触发面 wkv/src/session/raw/mod.rs:96-98 与 wkv/src/session/raw/write/append.rs:16/:35（PageNotReady 重试）；wedb/wkv/src/store/flush.rs:185-207 FlushStep::step（target=tail）、:149-155 flush_and_evict_all；wedb/whlog/src/hlog/shift.rs:113-163 shift_begin_address（:130 先 shift_ro、:132-138 未等 safe_ro 即 flush_addr_range）；契约自述 wedb/whlog/src/hlog/io.rs:215-219（flush_pages_range 文档）；先分配后写协议 wedb/whlog/src/hlog/append.rs:104-114 与 :160-183、encode_at 无锁 wedb/whlog/src/hlog/mod.rs:490-505、fill 闭包仅页读锁与页号校验 wedb/whlog/src/hlog/io.rs:272-286；正确范例 wedb/wcpr/src/manager/create.rs:214-227（shift_ro → wait safe_ro → flush_all）
  C#：libs/storage/Tsavorite/cs/src/core/Allocator/AllocatorBase.cs:1644-1652 ShiftReadOnlyAddress（epoch.BumpCurrentEpoch 包裹 OnPagesMarkedReadOnly）；:1744-1758 OnPagesMarkedReadOnly（Seal + Flush，SafeReadOnlyAddress 推进后 AsyncFlushPagesForReadOnly）；:2116-2123 AsyncFlushPagesForReadOnly（"Called when all threads have agreed that a page range is sealed"）；:1655-1707 ShiftBeginAddress（:1667 先 ShiftReadOnlyAddress 封存、:1669-1693 等 FlushedUntil 达标后才 :1696 推进 head——rust 补刷臂缺的正是该次序中的排空语义）
