重复：task/ing/checkpoint-dir-retention-wiring.md（关键符号 purge_outdated/purge_checkpoint_files_except/CleanupIndexCheckpoint 命中）
优先级：中
检查点目录无任何保留/清理接线：wcpr purge_outdated 全仓零生产调用，主库快照文件集随每次检查点无限堆积
  具体问题：wcpr/src/manager/mod.rs:510 purge_outdated 的文档自述「每次成功恢复或周期性快照后调用，即可将检查点磁盘占用约束在 keep 个版本之内」，但全仓生产零调用（grep 仅命中 wcpr 定义/导出与 wcpr 测试）；purge_all 同样零生产调用。主库侧唯一的清理形态是复制域 CheckpointStore 的副本侧 purge_checkpoint_files_except（wedb/src/server/replication/checkpoint_store.rs:18、replica_diskbased_sync.rs:195），不覆盖主库。主库每次 SAVE/BGSAVE/AOF 体积超限检查点都在 checkpoint 目录新增一组 index_<token>.ckpt（哈希索引全量快照）+ <token>/ 子目录（RangeIndex 树快照），从不回收；恢复链（database_manager_base.rs recover_database_checkpoint_async、wnode service.rs open_recovered 族）恢复成功后也无清理动作。AOF 超限任务（wnode/src/service.rs:516 spawn_aof_size_limit_task）按 frequency_secs 周期驱动，长期运行下快照目录随检查点次数线性增长直至耗尽磁盘。C# 对照两道清理齐备：检查点状态机完成后即调 CleanupIndexCheckpoint/CleanupLogCheckpoint（tokenHistory 环形队列每次拍新删前一代文件，removeOutdated 默认 true），恢复完成后 OnRecovery 再清全部未用 checkpoint
  rust：wedb/wcpr/src/manager/mod.rs:510 purge_outdated（零生产调用）、:485 purge_all（零生产调用）；堆积生产面 wnode/src/database/database_manager_base.rs:186-250 take_database_checkpoint_async（拍后无清理）、wnode/src/service.rs:516-535 spawn_aof_size_limit_task（周期触发源）；副本侧清理面（不覆盖主库）wedb/src/server/replication/checkpoint_store.rs:18
  C#：libs/storage/Tsavorite/cs/src/core/Index/Checkpointing/IndexCheckpointSMTask.cs:52（完成后 store.CleanupIndexCheckpoint）、HybridLogCheckpointSMTask.cs:67（完成后 store.CleanupLogCheckpoint）；libs/storage/Tsavorite/cs/src/core/Index/CheckpointManagement/DeviceLogCommitCheckpointManager.cs:224-233 CleanupIndexCheckpoint 与 :258-266 CleanupLogCheckpoint（tokenHistory 删前代）、:341-365 OnRecovery（恢复后清未用）
