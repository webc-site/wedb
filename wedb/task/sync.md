1. review ../../garnet，分析 Tsavorite BfTree 的公共底座模块、数据链路

2. 阅读 readme/zh.md ，理解我们如何把../garnet 底层引擎改写为 rust，并对照源代码，修订 zh.md en.md

3. 开子代理，思考我们的依赖关系，架构设计？

需要细化模块拆分吗？
需要优化改进的地方吗？
实现有缺失遗漏的吗？
有废弃需要清理的代码吗？
有冗余重复，可以拆分函数复用的代码吗？

让子代理对照 c#，分析数据链路，模块结构，深度思考

4. 按子代理的反馈，思考、拆分、优化、打磨

让子代理开 worktree 到/tmp/fork/下面，优化，写完、测试之后合并到当前目录

清理 worktree

如果遇到主分支修改，请提交，然后合并。

5. 并发对子包开子代理，对照 C# 审查模块代码实现

确保 rust 代码正确、完整、高效、简洁、优雅地实现了 C#的代码和相关测试

主代理负责调度，告知子代理对标哪些 c# 文件审查代码

子代理开 worktree 到 /tmp/fork/ 下面（参考第4步），优化

对于同一个模块，反复开子代理审查代码（循环）

直到子代理汇报已经正确、完备、高效、简洁、优雅地的实现的相关的 C# 代码和测试，代码达到生产级别

如果有问题，修订后，要检查上下游调用，确保调用链路和 c# 然后合并

代码要在注释中标注和 c#的文件、函数的对应关系，实现的异同

清理 hack 式编程，主体逻辑尽量参考 c#，但无需兼容 c#，清理为兼容 c# 写的代码

rust 要充分发挥自己的优势，在 c# 的基础上改进优化，为性能优化编码

编码规范见 .agents/skills/rust_review/SKILL.md

compio 是一个线程一个 cpu，利用这一点优化

要效验我们优化逻辑的正确性、空间复杂度、时间复杂度

键要是二进制安全的，相互隔离

凡事只有一种实现，不写冗余的别名（别名只用来简化复杂类型）

./test.sh 测试遇到问题，先翻阅 c#，思考它是怎么做的，对照修改

重复代码请拆分复用，模块设计低耦合、高内聚

序列化用 bitcode 或者 zerocopy ，hash 用 gxhash（这是稳定的 hash，与机器无关）

禁用 #[allow(dead_code)]，删除无用的代码

每当有子代理结束任务，都用中文汇报一下进展
